# 앱스토어 리뷰 요청(OS 네이티브 리뷰창) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 사용자가 일정을 5개 이상 저장했을 때, 일정 저장 직후 OS 네이티브 리뷰창을 설치당 1회 띄운다.

**Architecture:** 판정 로직은 플랫폼/저장소에 의존하지 않는 순수 함수(`app_review_policy.dart`)로 분리해 단위 테스트로 전부 커버한다. `AppReviewService`는 Riverpod `Provider`로 주입되며 provider 읽기(얇은 어댑터)와 판정·요청·기록(테스트 가능한 코어)을 분리한다. 노출은 `showEventEditor`가 에디터 페이지의 pop 결과를 받아 처리하므로 호출부 4곳은 수정하지 않는다.

**Tech Stack:** Flutter, Riverpod 3.3.2, `shared_preferences`, `in_app_review` 2.0.12, `firebase_analytics`

**설계 문서:** `docs/superpowers/specs/2026-08-10-앱스토어-리뷰요청-design.md`

## Global Constraints

- Dart SDK `>=3.11.0 <4.0.0`. 코드/주석은 기존 파일의 언어 관례를 따른다(설명 주석은 한국어).
- 판정 조건은 **저장된 일정 5개 이상**뿐이다. 설치 경과일·실행 횟수·사용일 조건은 넣지 않는다.
- 리뷰 요청은 **설치당 1회**. `review_requested_at` 키가 존재하면 다시 요청하지 않는다.
- 시각은 UTC ISO8601 문자열로 저장한다(기존 `BackupPrefs.writeNow`와 같은 형식).
- 웹(`kIsWeb`)에서는 아무것도 하지 않는다.
- 서버 킬스위치 `review.enabled`가 없거나 파싱 실패하면 **`true`(켜짐)** 로 동작한다.
- `openStoreListing` 폴백은 쓰지 않는다.
- 각 태스크 종료 시 `flutter analyze` 무경고, `flutter test` 전체 통과.
- `lib/app/app.dart`, `backup_notice.dart`, `backup_reminder.dart`는 건드리지 않는다.

## File Structure

| 파일 | 책임 |
|---|---|
| `lib/features/app_review/app_review_policy.dart` (신규) | 임계값 상수, 저장 키, 순수 판정 함수, 일정 개수 세기 |
| `lib/features/app_review/app_review_service.dart` (신규) | `appReviewServiceProvider`, provider 읽기 어댑터, 요청·기록, 애널리틱스 |
| `lib/features/app_metadata/app_metadata_service.dart` (수정) | `reviewPromptEnabled` 킬스위치 파싱 |
| `lib/features/events/presentation/event_editor_sheet.dart` (수정) | 신규 저장 시 pop(true), `showEventEditor`에서 리뷰 호출 |
| `pubspec.yaml` (수정) | `in_app_review` 의존성 |
| `test/app_review_policy_test.dart` (신규) | 판정 함수 경계값 |
| `test/app_review_service_test.dart` (신규) | 요청·기록 흐름 (fake `InAppReview`) |
| `test/app_metadata_service_test.dart` (수정) | 킬스위치 파싱 |

---

### Task 1: 판정 로직 (순수 함수)

플랫폼·저장소에 의존하지 않는 판정을 먼저 만든다. 이 태스크만으로 정책 전체가 테스트로 고정된다.

**Files:**
- Create: `lib/features/app_review/app_review_policy.dart`
- Test: `test/app_review_policy_test.dart`

**Interfaces:**
- Consumes: `CalendarEvent` (`lib/features/events/model/calendar_event.dart`)
- Produces:
  - `const int kReviewMinEventCount` (= 5)
  - `const String kReviewRequestedAtKey` (= `'review_requested_at'`)
  - `bool shouldRequestReview({required bool remoteEnabled, required DateTime? requestedAt, required int eventCount})`
  - `int countStoredEvents(Map<String, List<CalendarEvent>> events)`

- [ ] **Step 1: 실패하는 테스트 작성**

`test/app_review_policy_test.dart`:

```dart
import 'package:catholic_calendar/features/app_review/app_review_policy.dart';
import 'package:catholic_calendar/features/events/model/calendar_event.dart';
import 'package:flutter_test/flutter_test.dart';

CalendarEvent _event(String id) => CalendarEvent(
  id: id,
  date: '2026-08-10',
  categoryId: 'c1',
  categoryName: '본당 행사',
);

void main() {
  group('shouldRequestReview', () {
    test('일정이 5개 이상이면 요청한다', () {
      expect(
        shouldRequestReview(
          remoteEnabled: true,
          requestedAt: null,
          eventCount: 5,
        ),
        isTrue,
      );
    });

    test('일정이 4개면 요청하지 않는다', () {
      expect(
        shouldRequestReview(
          remoteEnabled: true,
          requestedAt: null,
          eventCount: 4,
        ),
        isFalse,
      );
    });

    test('일정이 5개를 넘어도 요청한다', () {
      expect(
        shouldRequestReview(
          remoteEnabled: true,
          requestedAt: null,
          eventCount: 12,
        ),
        isTrue,
      );
    });

    test('이미 요청한 적 있으면 조건을 만족해도 요청하지 않는다', () {
      expect(
        shouldRequestReview(
          remoteEnabled: true,
          requestedAt: DateTime.utc(2026, 8, 1),
          eventCount: 12,
        ),
        isFalse,
      );
    });

    test('킬스위치가 꺼져 있으면 요청하지 않는다', () {
      expect(
        shouldRequestReview(
          remoteEnabled: false,
          requestedAt: null,
          eventCount: 12,
        ),
        isFalse,
      );
    });
  });

  group('countStoredEvents', () {
    test('빈 맵은 0', () {
      expect(countStoredEvents(const {}), 0);
    });

    test('날짜별 리스트 길이를 모두 더한다', () {
      final events = {
        '2026-08-10': [_event('a'), _event('b')],
        '2026-08-11': [_event('c')],
      };

      expect(countStoredEvents(events), 3);
    });

    test('반복 일정도 엔트리 하나이므로 1개로 센다', () {
      final events = {
        '2026-08-10': [_event('yearly')],
      };

      expect(countStoredEvents(events), 1);
    });
  });
}
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `flutter test test/app_review_policy_test.dart`
Expected: FAIL - `Error: Couldn't resolve the package 'catholic_calendar/features/app_review/app_review_policy.dart'` 또는 `shouldRequestReview isn't defined`

- [ ] **Step 3: 구현**

`lib/features/app_review/app_review_policy.dart`:

```dart
import '../events/model/calendar_event.dart';

/// 리뷰를 요청하기 위해 필요한 최소 일정 개수.
///
/// 일정을 이만큼 채웠다는 것 자체를 "앱을 실제로 쓰고 있다"는 신호로 본다.
/// 설치 경과일이나 앱 실행 횟수는 보지 않는다.
const int kReviewMinEventCount = 5;

/// 리뷰 요청을 시도한 시각(UTC ISO8601). 값이 있으면 이 설치에서는 다시
/// 요청하지 않는다. 앱을 지우면 함께 사라지므로 재설치 시에는 다시 요청한다.
const String kReviewRequestedAtKey = 'review_requested_at';

/// OS 리뷰창을 요청할지 판정한다.
///
/// 시간 조건이 없으므로 현재 시각을 받지 않는다. 플랫폼 판별(`kIsWeb`)은
/// 호출부가 담당한다.
bool shouldRequestReview({
  required bool remoteEnabled,
  required DateTime? requestedAt,
  required int eventCount,
}) {
  if (requestedAt != null) return false; // 설치당 1회
  if (!remoteEnabled) return false; // 서버 킬스위치
  return eventCount >= kReviewMinEventCount;
}

/// 날짜별 일정 맵에 담긴 전체 일정 수.
///
/// 반복 일정은 저장된 엔트리가 하나이므로 회차 수와 무관하게 1개로 센다.
int countStoredEvents(Map<String, List<CalendarEvent>> events) =>
    events.values.fold<int>(0, (sum, list) => sum + list.length);
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `flutter test test/app_review_policy_test.dart`
Expected: PASS (8 tests)

- [ ] **Step 5: 정적 분석**

Run: `flutter analyze`
Expected: `No issues found!`

- [ ] **Step 6: 커밋**

```bash
git add lib/features/app_review/app_review_policy.dart test/app_review_policy_test.dart
git commit -m "리뷰 요청 판정 순수 함수 추가

일정 5개 이상 + 킬스위치 켜짐 + 미요청 상태일 때만 true.
시간 조건이 없어 현재 시각을 받지 않는다."
```

---

### Task 2: 서버 킬스위치 파싱

`/app/metadata` 응답의 `review.enabled`를 읽는다. 서버가 아직 이 필드를 안 내려주므로, **없을 때 켜짐으로 동작하는 것**이 이 태스크의 핵심이다.

**Files:**
- Modify: `lib/features/app_metadata/app_metadata_service.dart:15-31` (`AppMetadata` 클래스)
- Test: `test/app_metadata_service_test.dart` (기존 파일에 케이스 추가)

**Interfaces:**
- Produces: `AppMetadata.reviewPromptEnabled` (`bool`, 기본 `true`)

- [ ] **Step 1: 실패하는 테스트 추가**

`test/app_metadata_service_test.dart`의 `main()` 안에 아래 3개를 기존 테스트 뒤에 추가한다(기존 2개 테스트는 그대로 둔다):

```dart
  test('review.enabled가 false면 리뷰 요청을 끈다', () {
    final metadata = AppMetadata.fromJson({
      'review': {'enabled': false},
    });

    expect(metadata.reviewPromptEnabled, isFalse);
  });

  test('review 필드가 없으면 리뷰 요청은 켜짐', () {
    final metadata = AppMetadata.fromJson({
      'giftShop': {'url': 'https://example.com/gifts'},
    });

    expect(metadata.reviewPromptEnabled, isTrue);
  });

  test('review.enabled 타입이 잘못되면 켜짐으로 본다', () {
    final metadata = AppMetadata.fromJson({
      'review': {'enabled': 'nope'},
    });

    expect(metadata.reviewPromptEnabled, isTrue);
  });

  test('fallback은 리뷰 요청이 켜져 있다', () {
    expect(AppMetadata.fallback.reviewPromptEnabled, isTrue);
  });
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `flutter test test/app_metadata_service_test.dart`
Expected: FAIL - `The getter 'reviewPromptEnabled' isn't defined for the class 'AppMetadata'`

- [ ] **Step 3: 구현**

`lib/features/app_metadata/app_metadata_service.dart`의 `AppMetadata` 클래스를 아래로 교체한다:

```dart
class AppMetadata {
  const AppMetadata({
    required this.feastGiftShopUrl,
    this.reviewPromptEnabled = true,
  });

  final String feastGiftShopUrl;

  /// 앱스토어 리뷰 요청 킬스위치. 서버가 값을 안 내려주거나 형식이 틀리면
  /// 켜진 것으로 본다(서버 장애가 기능을 죽이지 않게).
  final bool reviewPromptEnabled;

  factory AppMetadata.fromJson(Map<String, dynamic> json) {
    final giftShop = json['giftShop'] as Map<String, dynamic>? ?? const {};
    final url = giftShop['url'] as String? ?? '';
    final review = json['review'] as Map<String, dynamic>? ?? const {};
    // `as bool?`가 아니라 `is bool`로 본다. 잘못된 타입을 캐스트하면 예외가
    // 나서, 킬스위치 오타 하나가 메타데이터 전체를 fallback으로 떨어뜨린다.
    final reviewEnabled = review['enabled'];
    return AppMetadata(
      feastGiftShopUrl: _safeHttpUrl(url) ?? kDefaultFeastGiftShopUrl,
      reviewPromptEnabled: reviewEnabled is bool ? reviewEnabled : true,
    );
  }

  static const fallback = AppMetadata(
    feastGiftShopUrl: kDefaultFeastGiftShopUrl,
  );
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `flutter test test/app_metadata_service_test.dart`
Expected: PASS (6 tests)

- [ ] **Step 5: 정적 분석**

Run: `flutter analyze`
Expected: `No issues found!`

- [ ] **Step 6: 커밋**

```bash
git add lib/features/app_metadata/app_metadata_service.dart test/app_metadata_service_test.dart
git commit -m "리뷰 요청 서버 킬스위치 파싱 추가

/app/metadata의 review.enabled를 읽는다. 필드가 없거나 타입이
틀리면 켜짐으로 본다 - 서버 장애가 기능을 죽이지 않도록."
```

---

### Task 3: 리뷰 요청 서비스

판정·요청·기록을 수행한다. provider를 읽는 얇은 어댑터(`maybeRequest`)와 테스트 가능한 코어(`runWith`)를 분리해, 코어만 단위 테스트한다.

**Files:**
- Modify: `pubspec.yaml` (`in_app_review` 추가)
- Create: `lib/features/app_review/app_review_service.dart`
- Test: `test/app_review_service_test.dart`

**Interfaces:**
- Consumes:
  - Task 1의 `shouldRequestReview`, `countStoredEvents`, `kReviewRequestedAtKey`
  - Task 2의 `AppMetadata.reviewPromptEnabled`
  - `sharedPreferencesProvider`, `eventStoreProvider` (`lib/features/events/application/event_providers.dart`)
  - `appMetadataProvider` (`lib/features/app_metadata/app_metadata_service.dart`)
- Produces:
  - `final Provider<AppReviewService> appReviewServiceProvider`
  - `class AppReviewService`
    - `AppReviewService(Ref ref, {InAppReview? review})`
    - `Future<void> maybeRequest()`
    - `Future<bool> runWith({required SharedPreferences prefs, required bool remoteEnabled, required int eventCount})`

- [ ] **Step 1: 의존성 추가**

`pubspec.yaml`의 `dependencies:` 블록에서 `app_links: ^7.2.1` 바로 아래 줄에 추가한다:

```yaml
  in_app_review: ^2.0.12
```

Run: `flutter pub get`
Expected: `Got dependencies!`

- [ ] **Step 2: 실패하는 테스트 작성**

`test/app_review_service_test.dart`:

```dart
import 'package:catholic_calendar/features/app_review/app_review_policy.dart';
import 'package:catholic_calendar/features/app_review/app_review_service.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:in_app_review/in_app_review.dart';
import 'package:shared_preferences/shared_preferences.dart';

/// `openStoreListing` 등 우리가 쓰지 않는 멤버는 noSuchMethod로 넘긴다.
/// 이렇게 하면 패키지가 메서드를 추가해도 이 fake가 깨지지 않는다.
class _FakeInAppReview implements InAppReview {
  _FakeInAppReview({this.available = true, this.throwOnRequest = false});

  final bool available;
  final bool throwOnRequest;
  int requestCount = 0;

  @override
  Future<bool> isAvailable() async => available;

  @override
  Future<void> requestReview() async {
    if (throwOnRequest) throw Exception('platform channel unavailable');
    requestCount += 1;
  }

  @override
  dynamic noSuchMethod(Invocation invocation) =>
      super.noSuchMethod(invocation);
}

void main() {
  setUp(() => SharedPreferences.setMockInitialValues({}));

  Future<(AppReviewService, _FakeInAppReview, SharedPreferences)> setUpService({
    bool available = true,
    bool throwOnRequest = false,
  }) async {
    final review = _FakeInAppReview(
      available: available,
      throwOnRequest: throwOnRequest,
    );
    final container = ProviderContainer(
      overrides: [
        appReviewServiceProvider.overrideWith(
          (ref) => AppReviewService(ref, review: review),
        ),
      ],
    );
    addTearDown(container.dispose);
    final prefs = await SharedPreferences.getInstance();
    return (container.read(appReviewServiceProvider), review, prefs);
  }

  test('조건을 만족하면 리뷰를 요청하고 시각을 기록한다', () async {
    final (service, review, prefs) = await setUpService();

    final requested = await service.runWith(
      prefs: prefs,
      remoteEnabled: true,
      eventCount: 5,
    );

    expect(requested, isTrue);
    expect(review.requestCount, 1);
    expect(prefs.getString(kReviewRequestedAtKey), isNotNull);
  });

  test('일정이 모자라면 요청하지도, 기록하지도 않는다', () async {
    final (service, review, prefs) = await setUpService();

    final requested = await service.runWith(
      prefs: prefs,
      remoteEnabled: true,
      eventCount: 4,
    );

    expect(requested, isFalse);
    expect(review.requestCount, 0);
    expect(prefs.getString(kReviewRequestedAtKey), isNull);
  });

  test('이미 요청한 뒤에는 다시 요청하지 않는다', () async {
    final (service, review, prefs) = await setUpService();
    await prefs.setString(
      kReviewRequestedAtKey,
      DateTime.utc(2026, 8, 1).toIso8601String(),
    );

    final requested = await service.runWith(
      prefs: prefs,
      remoteEnabled: true,
      eventCount: 10,
    );

    expect(requested, isFalse);
    expect(review.requestCount, 0);
  });

  test('킬스위치가 꺼져 있으면 요청하지 않는다', () async {
    final (service, review, prefs) = await setUpService();

    final requested = await service.runWith(
      prefs: prefs,
      remoteEnabled: false,
      eventCount: 10,
    );

    expect(requested, isFalse);
    expect(review.requestCount, 0);
  });

  test('리뷰창을 띄울 수 없으면 기록을 남기지 않는다', () async {
    final (service, review, prefs) = await setUpService(available: false);

    final requested = await service.runWith(
      prefs: prefs,
      remoteEnabled: true,
      eventCount: 10,
    );

    expect(requested, isFalse);
    expect(review.requestCount, 0);
    // 설치당 1회뿐인 기회를 못 띄운 상황에 소모하지 않는다.
    expect(prefs.getString(kReviewRequestedAtKey), isNull);
  });

  test('요청이 예외를 던지면 기록하지 않고 삼킨다', () async {
    final (service, _, prefs) = await setUpService(throwOnRequest: true);

    final requested = await service.runWith(
      prefs: prefs,
      remoteEnabled: true,
      eventCount: 10,
    );

    expect(requested, isFalse);
    expect(prefs.getString(kReviewRequestedAtKey), isNull);
  });
}
```

- [ ] **Step 3: 테스트가 실패하는지 확인**

Run: `flutter test test/app_review_service_test.dart`
Expected: FAIL - `Couldn't resolve the package 'catholic_calendar/features/app_review/app_review_service.dart'`

- [ ] **Step 4: 구현**

`lib/features/app_review/app_review_service.dart`:

```dart
import 'package:firebase_analytics/firebase_analytics.dart';
import 'package:firebase_core/firebase_core.dart';
import 'package:flutter/foundation.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:in_app_review/in_app_review.dart';
import 'package:shared_preferences/shared_preferences.dart';

import '../app_metadata/app_metadata_service.dart';
import '../events/application/event_providers.dart';
import 'app_review_policy.dart';

final appReviewServiceProvider = Provider<AppReviewService>(
  AppReviewService.new,
);

/// OS 네이티브 리뷰창을 조건부로 1회 요청한다.
///
/// 일정 저장 직후에만 호출한다(`showEventEditor`). 앱 시작 시점에는 띄우지
/// 않는다 - 앱을 열어보기만 한 사용자에게 리뷰를 청하지 않기 위해서다.
class AppReviewService {
  AppReviewService(this._ref, {InAppReview? review}) : _review = review;

  final Ref _ref;
  final InAppReview? _review;

  /// provider에서 값을 모아 [runWith]에 넘기는 얇은 어댑터.
  Future<void> maybeRequest() async {
    if (kIsWeb) return;

    final prefs = await _ref.read(sharedPreferencesProvider.future);
    final metadata = await _ref.read(appMetadataProvider.future);
    final events = await _ref.read(eventStoreProvider.future);

    final requested = await runWith(
      prefs: prefs,
      remoteEnabled: metadata.reviewPromptEnabled,
      eventCount: countStoredEvents(events),
    );

    // 이 이벤트는 "요청 시도" 수다. 리뷰창이 실제로 떴는지, 사용자가 별점을
    // 매겼는지는 OS가 알려주지 않아 측정할 수 없다.
    if (requested && Firebase.apps.isNotEmpty) {
      await FirebaseAnalytics.instance.logEvent(name: 'app_review_requested');
    }
  }

  /// 판정 → 요청 → 기록. 실제로 요청을 보냈으면 true.
  ///
  /// provider에 의존하지 않아 단위 테스트에서 직접 호출한다.
  @visibleForTesting
  Future<bool> runWith({
    required SharedPreferences prefs,
    required bool remoteEnabled,
    required int eventCount,
  }) async {
    final raw = prefs.getString(kReviewRequestedAtKey);
    final requestedAt = (raw == null || raw.isEmpty)
        ? null
        : DateTime.tryParse(raw);

    if (!shouldRequestReview(
      remoteEnabled: remoteEnabled,
      requestedAt: requestedAt,
      eventCount: eventCount,
    )) {
      return false;
    }

    final review = _review ?? InAppReview.instance;
    try {
      // 띄울 수 없는 상황이면 기록을 남기지 않는다. 설치당 1회뿐인 기회를
      // 리뷰창이 뜨지 못한 상황에 소모하면 안 된다.
      if (!await review.isAvailable()) return false;
      await review.requestReview();
    } catch (error) {
      debugPrint('[KCC review] 리뷰 요청 실패: $error');
      return false;
    }

    await prefs.setString(
      kReviewRequestedAtKey,
      DateTime.now().toUtc().toIso8601String(),
    );
    return true;
  }
}
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `flutter test test/app_review_service_test.dart`
Expected: PASS (6 tests)

- [ ] **Step 6: 정적 분석과 전체 테스트**

Run: `flutter analyze && flutter test`
Expected: `No issues found!` + 전체 테스트 통과

- [ ] **Step 7: 커밋**

```bash
git add pubspec.yaml pubspec.lock lib/features/app_review/app_review_service.dart test/app_review_service_test.dart
git commit -m "리뷰 요청 서비스 추가

판정 -> 요청 -> 기록을 수행한다. provider를 읽는 어댑터(maybeRequest)와
테스트 가능한 코어(runWith)를 분리했다.

- isAvailable()이 false거나 예외가 나면 기록을 남기지 않는다.
  설치당 1회뿐인 기회를 못 띄운 상황에 소모하지 않기 위해서다.
- 애널리틱스는 '요청 시도' 수일 뿐이다. 실제 표시 여부는 OS가 알려주지 않는다."
```

---

### Task 4: 일정 저장 직후 노출 연결

에디터가 신규 저장 시 `pop(true)`를 반환하고, `showEventEditor`가 그 결과를 받아 리뷰를 요청한다. 호출부 4곳(`calendar_page.dart`, `day_detail_view.dart` 2곳, `app.dart`)은 **수정하지 않는다.**

**위젯 테스트를 추가하지 않는 이유:** 연결 코드에는 분기가 하나(`saved == true`)뿐이고, 실제 판정 로직 전체는 Task 1·3의 단위 테스트가 이미 커버한다. 이 경로를 위젯 테스트로 덮으려면 카테고리 선택 화면을 거쳐 저장까지 가는 긴 시나리오가 필요한데, 검증되는 것은 "함수가 불렸는가" 한 줄뿐이라 비용이 이득을 넘는다. 대신 기존 위젯 테스트가 깨지지 않는지 확인하고, 실기기 수동 검증 항목으로 남긴다.

**Files:**
- Modify: `lib/features/events/presentation/event_editor_sheet.dart:28-39` (`showEventEditor`), `:427` (`_save`의 pop)

**Interfaces:**
- Consumes: Task 3의 `appReviewServiceProvider`, `AppReviewService.maybeRequest()`
- Produces: 없음 (`showEventEditor`의 시그니처는 `Future<void>`로 유지)

- [ ] **Step 1: 신규 저장 시 저장 여부를 pop으로 돌려준다**

`event_editor_sheet.dart`의 `_save` 마지막 줄(427번째 줄 부근)을 바꾼다.

변경 전:
```dart
    if (mounted) Navigator.of(context).pop();
  }
```

변경 후:
```dart
    // 신규 저장일 때만 true를 돌려준다. showEventEditor가 이 값을 보고
    // 리뷰 요청 여부를 판단한다(수정/삭제/취소는 발동시키지 않는다).
    if (mounted) Navigator.of(context).pop(!_isEditing);
  }
```

`_delete`의 pop(453번째 줄 부근)과 그 밖의 pop은 **건드리지 않는다.** 값 없이 pop하면 `null`이 되어 리뷰가 발동하지 않는다.

- [ ] **Step 2: `showEventEditor`가 결과를 받아 리뷰를 요청하게 한다**

같은 파일 상단의 `showEventEditor`(28-39번째 줄)를 아래로 교체한다:

```dart
/// Opens the add/edit event screen. Pass [existing] to edit; otherwise a new
/// event is created on [date].
///
/// 신규 저장이 끝나면 앱스토어 리뷰 요청 조건을 확인한다(조건 미달이면 아무
/// 일도 일어나지 않는다). 호출부는 이를 신경 쓰지 않아도 된다.
Future<void> showEventEditor(
  BuildContext context, {
  required DateTime date,
  CalendarEvent? existing,
  SharedEventDraft? draft,
}) async {
  // pop 이후에도 유효해야 하므로 에디터가 아니라 호출부 context에서 얻는다.
  final container = ProviderScope.containerOf(context, listen: false);

  final saved = await Navigator.of(context).push<bool>(
    MaterialPageRoute<bool>(
      builder: (_) =>
          _EventEditorPage(date: date, existing: existing, draft: draft),
    ),
  );
  if (saved != true) return;

  // 페이지 전환이 완전히 끝난 뒤 띄운다.
  await Future<void>.delayed(const Duration(milliseconds: 400));
  await container.read(appReviewServiceProvider).maybeRequest();
}
```

- [ ] **Step 3: import 추가**

같은 파일 상단의 import 목록에서 `import '../../sharing/share_link.dart';` 바로 위에 추가한다:

```dart
import '../../app_review/app_review_service.dart';
```

- [ ] **Step 4: 정적 분석**

Run: `flutter analyze`
Expected: `No issues found!`

- [ ] **Step 5: 기존 테스트 회귀 확인**

Run: `flutter test`
Expected: 전체 통과. 특히 `test/event_editor_draft_test.dart`가 통과해야 한다(이 테스트는 저장까지 가지 않으므로 리뷰 경로를 타지 않는다).

- [ ] **Step 6: 커밋**

```bash
git add lib/features/events/presentation/event_editor_sheet.dart
git commit -m "일정 저장 직후 리뷰 요청 연결

에디터가 신규 저장 시 pop(true)를 반환하고, showEventEditor가 그 결과를
받아 리뷰를 요청한다. 호출부 4곳은 수정하지 않는다.

수정/삭제/취소는 값 없이 pop하므로 발동하지 않는다."
```

---

## 완료 후 확인

- [ ] `flutter analyze` 무경고
- [ ] `flutter test` 전체 통과
- [ ] 설계 문서의 "주요 파일" 목록과 실제 변경 파일이 일치하는지 대조
- [ ] `lib/app/app.dart`, `backup_notice.dart`, `backup_reminder.dart`가 변경되지 않았는지 `git diff --stat`으로 확인

## 이 계획에 포함되지 않은 것 (사용자 몫)

- 서버 `/app/metadata` 응답에 `"review": {"enabled": true}` 추가. 앱은 필드가 없으면 켜짐으로 동작하므로 이 작업 없이도 배포 가능하지만, 문제 발생 시 재배포 없이 끄려면 필요하다.
- 실기기 검증: 일정 4개까지는 안 뜨고 5개째에 뜨는지, 그 뒤 6개째에는 안 뜨는지. iOS는 TestFlight/디버그 빌드에서만 항상 뜨고 프로덕션에서는 시스템 쿼터가 적용된다.
- 배포 후 App Store Connect / Play Console에서 리뷰 수 추이 확인.
