# 일정 알림 리드타임(사용자 지정 + 최대 2개) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 개인 일정/축일의 알림 시점을 사용자가 8종(5분~1주일 전) 중 고르고, 한 일정에 알림을 최대 2개까지 둘 수 있게 한다.

**Architecture:** `ReminderLead` enum(리드타임 + 알림 시각 계산). `CalendarEvent`에 `List<ReminderLead> reminders` 추가(`notify`는 마스터 on/off 유지). 알림 스케줄러가 각 회차 x 리드마다 시각을 계산해 예약. 재사용 위젯 `ReminderEditor`가 두 편집기(일정/축일)에서 알림 행(최대 2)+선택 시트를 렌더.

**Tech Stack:** Flutter, Riverpod 3, `flutter_local_notifications`/`timezone`, SharedPreferences(간접).

## Global Constraints
- 주석·UI 문자열: 특수 대시(—, –) 금지, 일반 하이픈(-). 새 서드파티 의존성 금지.
- 리드타임: 5분/10분/30분/1시간/2시간/1일/2일/1주일 전.
- 혼합 기준: 분/시간 = 일정 시작 시각 기준(`start - Duration`); 일/주 = (해당 날 - N일) 09:00.
- 종일 일정(time==null): 분/시간 리드 미제공(일/주만). sub-day 리드의 `reminderTime`은 종일에 null.
- 최대 2개, 기본 `[day1]`. 하위호환: `reminders` 없으면 `[day1]`; `notify`는 유지.
- 일정 편집기 + 축일 편집기 공통(축일은 항상 종일 -> 일/주만).
- `flutter analyze` 무경고, `flutter test` 통과.

---

## 파일 구조
- 신규 `lib/features/events/model/reminder_lead.dart` - enum + `reminderTime`.
- 신규 `lib/features/events/presentation/reminder_editor.dart` - `ReminderEditor` 위젯(행/선택시트/추가삭제).
- 수정 `lib/features/events/model/calendar_event.dart` - `reminders` 필드/직렬화/copyWith.
- 수정 `lib/features/events/notifications/notifications_mobile.dart` - `_remindersFor` 재작성.
- 수정 `lib/features/events/presentation/event_editor_sheet.dart` - 알림 섹션에 ReminderEditor.
- 수정 `lib/features/saints/presentation/saint_feast_editor_page.dart` - 알림 섹션에 ReminderEditor(종일).
- 신규 테스트: `test/reminder_lead_test.dart`, `test/calendar_event_reminders_test.dart`, `test/reminder_editor_test.dart`.

---

## Task 1: ReminderLead enum + reminderTime

**Files:** Create `lib/features/events/model/reminder_lead.dart`; Test `test/reminder_lead_test.dart`.

**Interfaces (Produces):** `enum ReminderLead { min5, min10, min30, hour1, hour2, day1, day2, week1 }` with `displayName`, `isSubDay`, `storageValue`, `static fromStorage`, `DateTime? reminderTime(DateTime date, String? time)`.

- [ ] **Step 1: 실패 테스트**

Create `test/reminder_lead_test.dart`:
```dart
import 'package:catholic_calendar/features/events/model/reminder_lead.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('분/시간 리드는 시작 시각 기준', () {
    expect(ReminderLead.hour1.reminderTime(DateTime(2026, 7, 28), '15:00'),
        DateTime(2026, 7, 28, 14, 0));
    expect(ReminderLead.min30.reminderTime(DateTime(2026, 7, 28), '15:00'),
        DateTime(2026, 7, 28, 14, 30));
    // 자정 넘김
    expect(ReminderLead.hour2.reminderTime(DateTime(2026, 7, 28), '00:30'),
        DateTime(2026, 7, 27, 22, 30));
  });

  test('일/주 리드는 해당 날 -N일 09:00', () {
    expect(ReminderLead.day1.reminderTime(DateTime(2026, 7, 28), '15:00'),
        DateTime(2026, 7, 27, 9, 0));
    expect(ReminderLead.day2.reminderTime(DateTime(2026, 7, 28), null),
        DateTime(2026, 7, 26, 9, 0));
    expect(ReminderLead.week1.reminderTime(DateTime(2026, 7, 28), null),
        DateTime(2026, 7, 21, 9, 0));
  });

  test('종일 일정에는 분/시간 리드 없음(null)', () {
    expect(ReminderLead.min5.reminderTime(DateTime(2026, 7, 28), null), isNull);
    expect(ReminderLead.hour1.reminderTime(DateTime(2026, 7, 28), null), isNull);
  });

  test('isSubDay / fromStorage 왕복', () {
    expect(ReminderLead.hour2.isSubDay, isTrue);
    expect(ReminderLead.day1.isSubDay, isFalse);
    expect(ReminderLead.fromStorage('week1'), ReminderLead.week1);
    expect(ReminderLead.fromStorage('nope'), isNull);
  });
}
```

- [ ] **Step 2: 실패 확인** — `flutter test test/reminder_lead_test.dart` (undefined).

- [ ] **Step 3: 구현**

Create `lib/features/events/model/reminder_lead.dart` - 모든 멤버를 **enum 본문**에 정의한다(extension 사용 안 함):
```dart
/// 일/주 단위 알림이 울리는 고정 시각(아침 9시).
const int kReminderDayHour = 9;

/// 일정 알림 리드타임. 분/시간 단위는 일정 시작 시각 기준, 일/주 단위는 해당 날 아침 9시.
enum ReminderLead {
  min5,
  min10,
  min30,
  hour1,
  hour2,
  day1,
  day2,
  week1;

  static ReminderLead? fromStorage(String? raw) {
    for (final v in values) {
      if (v.name == raw) return v;
    }
    return null;
  }

  String get displayName => switch (this) {
    min5 => '5분 전',
    min10 => '10분 전',
    min30 => '30분 전',
    hour1 => '1시간 전',
    hour2 => '2시간 전',
    day1 => '1일 전',
    day2 => '2일 전',
    week1 => '1주일 전',
  };

  bool get isSubDay => switch (this) {
    min5 || min10 || min30 || hour1 || hour2 => true,
    day1 || day2 || week1 => false,
  };

  Duration get _subDayDuration => switch (this) {
    min5 => const Duration(minutes: 5),
    min10 => const Duration(minutes: 10),
    min30 => const Duration(minutes: 30),
    hour1 => const Duration(hours: 1),
    hour2 => const Duration(hours: 2),
    _ => Duration.zero,
  };

  int get _dayOffset => switch (this) {
    day1 => 1,
    day2 => 2,
    week1 => 7,
    _ => 0,
  };

  String get storageValue => name;

  /// 이 리드의 알림 시각. [date]=발생일(날짜만), [time]='HH:mm' 또는 null(종일).
  /// 분/시간 리드는 종일 일정엔 무의미하므로 null.
  DateTime? reminderTime(DateTime date, String? time) {
    if (isSubDay) {
      if (time == null) return null;
      final p = time.split(':');
      final h = int.tryParse(p[0]) ?? kReminderDayHour;
      final m = p.length > 1 ? (int.tryParse(p[1]) ?? 0) : 0;
      final start = DateTime(date.year, date.month, date.day, h, m);
      return start.subtract(_subDayDuration);
    }
    return DateTime(
      date.year,
      date.month,
      date.day - _dayOffset,
      kReminderDayHour,
      0,
    );
  }
}
```

- [ ] **Step 4: 통과 확인** — `flutter test test/reminder_lead_test.dart` (PASS).

- [ ] **Step 5: analyze + 커밋**
```bash
flutter analyze lib/features/events/model/reminder_lead.dart
git add lib/features/events/model/reminder_lead.dart test/reminder_lead_test.dart
git commit -m "알림 리드타임 ReminderLead enum + 시각 계산 추가"
```

---

## Task 2: CalendarEvent.reminders

**Files:** Modify `lib/features/events/model/calendar_event.dart`; Test `test/calendar_event_reminders_test.dart`.

**Interfaces (Produces):** `CalendarEvent.reminders: List<ReminderLead>` (기본 `[ReminderLead.day1]`), copyWith/toJson/fromJson 반영.

- [ ] **Step 1: 실패 테스트**

Create `test/calendar_event_reminders_test.dart`:
```dart
import 'package:catholic_calendar/features/events/model/calendar_event.dart';
import 'package:catholic_calendar/features/events/model/reminder_lead.dart';
import 'package:flutter_test/flutter_test.dart';

CalendarEvent _base({List<ReminderLead>? reminders}) => CalendarEvent(
  id: 'e1', date: '2026-07-28', categoryId: 'c1', categoryName: '가족',
  reminders: reminders ?? const [ReminderLead.day1],
);

void main() {
  test('reminders 왕복', () {
    final e = _base(reminders: const [ReminderLead.hour1, ReminderLead.day1]);
    final r = CalendarEvent.fromJson(e.toJson());
    expect(r.reminders, const [ReminderLead.hour1, ReminderLead.day1]);
  });
  test('기본값 [day1]', () {
    expect(_base().reminders, const [ReminderLead.day1]);
  });
  test('하위호환: reminders 없으면 [day1]', () {
    final json = {
      'id': 'x', 'date': '2026-07-28', 'categoryId': 'c1',
      'categoryName': '가족', 'type': 'regular', 'notify': true,
    };
    expect(CalendarEvent.fromJson(json).reminders, const [ReminderLead.day1]);
  });
  test('빈 리스트도 [day1]로 보정', () {
    final json = {
      'id': 'x', 'date': '2026-07-28', 'categoryId': 'c1',
      'categoryName': '가족', 'type': 'regular', 'reminders': <String>[],
    };
    expect(CalendarEvent.fromJson(json).reminders, const [ReminderLead.day1]);
  });
  test('copyWith', () {
    expect(_base().copyWith(reminders: const [ReminderLead.week1]).reminders,
        const [ReminderLead.week1]);
  });
}
```

- [ ] **Step 2: 실패 확인** — `flutter test test/calendar_event_reminders_test.dart`.

- [ ] **Step 3: 구현** (`calendar_event.dart`)

import 추가 (파일 상단, recurrence import 근처):
```dart
import 'reminder_lead.dart';
```
생성자에 파라미터 추가(`this.feastId,` 다음):
```dart
    this.reminders = const [ReminderLead.day1],
```
필드 선언 추가(`feastId` 필드 근처):
```dart
  /// 알림 리드타임 목록(최대 2). notify가 켜졌을 때만 예약된다. 기본 [1일 전].
  final List<ReminderLead> reminders;
```
`copyWith` 시그니처에 `List<ReminderLead>? reminders,` 추가, 반환에 `reminders: reminders ?? this.reminders,` 추가.

`toJson`에 추가(`feastId` 라인 근처):
```dart
    'reminders': [for (final r in reminders) r.name],
```

`fromJson`: 반환 CalendarEvent 생성 전에 파싱, 없으면 [day1]:
```dart
    final remindersRaw = json['reminders'] as List?;
    final reminders = remindersRaw == null
        ? const [ReminderLead.day1]
        : (remindersRaw
                  .map((e) => ReminderLead.fromStorage(e as String?))
                  .whereType<ReminderLead>()
                  .toList());
    final safeReminders =
        reminders.isEmpty ? const [ReminderLead.day1] : reminders;
```
그리고 `return CalendarEvent(...)`에 `reminders: safeReminders,` 추가.

- [ ] **Step 4: 통과 + 회귀** — `flutter test test/calendar_event_reminders_test.dart` 그리고 `flutter test`(기존 직렬화 테스트 회귀 없음).

- [ ] **Step 5: analyze + 커밋**
```bash
flutter analyze lib/features/events/model/calendar_event.dart
git add lib/features/events/model/calendar_event.dart test/calendar_event_reminders_test.dart
git commit -m "CalendarEvent에 알림 리드타임 목록(reminders) 추가"
```

---

## Task 3: 알림 스케줄러 재작성

**Files:** Modify `lib/features/events/notifications/notifications_mobile.dart`.

- [ ] **Step 1: import + 상수 정리**

import 추가:
```dart
import '../model/reminder_lead.dart';
```
`_dayBeforeHour` 상수는 더 이상 쓰지 않으므로 제거. `_dayOfHour`도 `_remindersFor`에서 안 쓰면 제거
(reminderTime이 09:00을 자체 처리). analyze 경고가 나면 제거로 해소.

- [ ] **Step 2: `_remindersFor` 교체**

현재 `_remindersFor` 전체(시그니처부터 닫는 `}`까지, dayOf/dayBefore 로직)를 아래로 교체:
```dart
  /// 이벤트의 미래 알림들. 각 발생일(회차) x 선택된 리드마다 시각을 계산한다.
  Iterable<_Reminder> _remindersFor(
    CalendarEvent e,
    tz.TZDateTime now,
    RecurrenceExpander expander,
  ) {
    final timeLabel = e.isAllDay ? '종일' : e.time!;
    final suffix = (e.memo != null && e.memo!.trim().isNotEmpty)
        ? ' · ${e.memo!.trim()}'
        : '';
    final typeLabel = e.isSaintFeast ? '축일' : '일정';
    final title = '$typeLabel · ${e.title}';
    final body = '$timeLabel$suffix';

    // 발생일: 비반복은 앵커 1개, 반복은 오늘 이후 다음 K회차.
    final List<DateTime> occurrences = e.isRecurring
        ? expander.nextOccurrences(
            e,
            DateTime(now.year, now.month, now.day),
            _maxOccurrencesPerEvent,
          )
        : [parseEventDate(e.date)];

    final reminders = <_Reminder>[];
    for (final date in occurrences) {
      for (final lead in e.reminders) {
        final when = lead.reminderTime(date, e.time);
        if (when == null) continue; // 종일 + 분/시간 리드
        final tzWhen = tz.TZDateTime(
          tz.local,
          when.year,
          when.month,
          when.day,
          when.hour,
          when.minute,
        );
        if (tzWhen.isAfter(now)) reminders.add(_Reminder(tzWhen, title, body));
      }
    }
    return reminders;
  }
```
(sync에서 `if (!e.notify) continue;`는 그대로 유지 -> notify가 마스터 스위치.)

- [ ] **Step 3: analyze + 전체 테스트** — `flutter analyze` 무경고(미사용 상수 제거 확인), `flutter test` 통과.

- [ ] **Step 4: 커밋**
```bash
git add lib/features/events/notifications/notifications_mobile.dart
git commit -m "알림 스케줄러를 리드타임(reminders) 기반으로 재작성"
```

---

## Task 4: ReminderEditor 위젯(재사용) + 선택 시트

**Files:** Create `lib/features/events/presentation/reminder_editor.dart`; Test `test/reminder_editor_test.dart`.

**Interfaces (Produces):** `class ReminderEditor extends StatelessWidget` (`reminders`, `allDay`, `max=2`, `onChanged`).

- [ ] **Step 1: 실패 위젯 테스트**

Create `test/reminder_editor_test.dart`:
```dart
import 'package:catholic_calendar/features/events/model/reminder_lead.dart';
import 'package:catholic_calendar/features/events/presentation/reminder_editor.dart';
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

Future<void> _pump(WidgetTester t, List<ReminderLead> value,
    {required bool allDay, required ValueChanged<List<ReminderLead>> onChanged}) {
  return t.pumpWidget(MaterialApp(
    home: Scaffold(
      body: ReminderEditor(reminders: value, allDay: allDay, onChanged: onChanged),
    ),
  ));
}

void main() {
  testWidgets('기본 1개 + 알림 추가로 2개, 최대 2', (t) async {
    var value = const [ReminderLead.day1];
    await _pump(t, value, allDay: false, onChanged: (v) => value = v);
    expect(find.text('알림 1'), findsOneWidget);
    expect(find.text('알림 추가'), findsOneWidget);

    await t.tap(find.text('알림 추가'));
    await _pump(t, value, allDay: false, onChanged: (v) => value = v);
    await t.pump();
    expect(value.length, 2);
  });

  testWidgets('종일이면 선택 시트에 분/시간 리드 없음', (t) async {
    await _pump(t, const [ReminderLead.day1], allDay: true, onChanged: (_) {});
    await t.tap(find.text('알림 1'));
    await t.pumpAndSettle();
    expect(find.text('1주일 전'), findsOneWidget); // 일/주 있음
    expect(find.text('5분 전'), findsNothing); // 분/시간 없음
  });
}
```

- [ ] **Step 2: 실패 확인** — `flutter test test/reminder_editor_test.dart`.

- [ ] **Step 3: 구현**

Create `lib/features/events/presentation/reminder_editor.dart`:
```dart
import 'package:flutter/material.dart';

import '../model/reminder_lead.dart';

/// 알림 리드타임 목록 편집기(최대 [max]개). 종일이면 분/시간 리드는 숨긴다.
/// 상태는 상위가 소유([reminders]) - 변경 시 [onChanged]로 새 목록 전달.
class ReminderEditor extends StatelessWidget {
  const ReminderEditor({
    super.key,
    required this.reminders,
    required this.allDay,
    required this.onChanged,
    this.max = 2,
  });

  final List<ReminderLead> reminders;
  final bool allDay;
  final ValueChanged<List<ReminderLead>> onChanged;
  final int max;

  List<ReminderLead> get _available => allDay
      ? ReminderLead.values.where((l) => !l.isSubDay).toList()
      : ReminderLead.values.toList();

  Future<void> _pick(BuildContext context, int index) async {
    final picked = await showModalBottomSheet<ReminderLead>(
      context: context,
      showDragHandle: true,
      builder: (ctx) => SafeArea(
        child: Column(
          mainAxisSize: MainAxisSize.min,
          children: [
            for (final l in _available)
              ListTile(
                title: Text(l.displayName),
                trailing: reminders[index] == l ? const Icon(Icons.check) : null,
                onTap: () => Navigator.of(ctx).pop(l),
              ),
          ],
        ),
      ),
    );
    if (picked != null) {
      final next = [...reminders];
      next[index] = picked;
      onChanged(next);
    }
  }

  void _add() {
    final used = reminders.toSet();
    final candidate = _available.firstWhere(
      (l) => !used.contains(l),
      orElse: () => _available.first,
    );
    onChanged([...reminders, candidate]);
  }

  void _remove(int index) {
    final next = [...reminders]..removeAt(index);
    onChanged(next.isEmpty ? const [ReminderLead.day1] : next);
  }

  @override
  Widget build(BuildContext context) {
    final theme = Theme.of(context);
    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        for (var i = 0; i < reminders.length; i++)
          ListTile(
            contentPadding: const EdgeInsets.only(left: 40, right: 8),
            title: Text('알림 ${i + 1}'),
            trailing: Row(
              mainAxisSize: MainAxisSize.min,
              children: [
                Text(
                  reminders[i].displayName,
                  style: theme.textTheme.bodyMedium?.copyWith(
                    color: theme.colorScheme.onSurfaceVariant,
                  ),
                ),
                if (reminders.length > 1)
                  IconButton(
                    icon: const Icon(Icons.close, size: 18),
                    tooltip: '삭제',
                    onPressed: () => _remove(i),
                  )
                else
                  const Icon(Icons.chevron_right),
              ],
            ),
            onTap: () => _pick(context, i),
          ),
        if (reminders.length < max)
          Padding(
            padding: const EdgeInsets.only(left: 40),
            child: Align(
              alignment: Alignment.centerLeft,
              child: TextButton.icon(
                onPressed: _add,
                icon: const Icon(Icons.add, size: 18),
                label: const Text('알림 추가'),
              ),
            ),
          ),
      ],
    );
  }
}

/// 종일 여부에 맞게 리드 목록을 보정(종일이면 분/시간 제거). 비면 [day1].
List<ReminderLead> sanitizeReminders(List<ReminderLead> reminders, {required bool allDay}) {
  final filtered = allDay ? reminders.where((l) => !l.isSubDay).toList() : reminders.toList();
  return filtered.isEmpty ? const [ReminderLead.day1] : filtered;
}
```

- [ ] **Step 4: 통과 + analyze + 커밋** — `flutter test test/reminder_editor_test.dart`, `flutter analyze`, `flutter test`(전체).
```bash
git add lib/features/events/presentation/reminder_editor.dart test/reminder_editor_test.dart
git commit -m "알림 리드타임 편집 위젯 ReminderEditor 추가"
```

---

## Task 5: 두 편집기에 알림 섹션 연결

**Files:** Modify `event_editor_sheet.dart`, `saint_feast_editor_page.dart`.

**Interfaces (Consumes):** `ReminderEditor`, `sanitizeReminders`, `ReminderLead`(Task 1/4).

- [ ] **Step 1: event_editor_sheet 연결**

import 추가:
```dart
import '../model/reminder_lead.dart';
import 'reminder_editor.dart';
```
상태 추가(`_notify` 근처):
```dart
  late List<ReminderLead> _reminders;
```
initState(`_notify = e?.notify ?? true;` 다음):
```dart
    _reminders = sanitizeReminders(
      e?.reminders ?? const [ReminderLead.day1],
      allDay: (e?.time) == null ? (widget.existing != null ? true : (_time == null)) : false,
    );
```
(간단히: `_reminders = sanitizeReminders([...?e?.reminders].isEmpty ? const [ReminderLead.day1] : e!.reminders, allDay: _time == null);` - _time 초기화 이후에 두거나, initState 끝에서 `_reminders = sanitizeReminders(_reminders, allDay: _time == null);`로 보정.)

`_setAllDay`(종일 토글) 내부 setState에 추가:
```dart
      _reminders = sanitizeReminders(_reminders, allDay: value);
```

build의 알림 SwitchListTile 바로 아래에 조건부로 ReminderEditor 삽입:
```dart
            SwitchListTile(
              ... 기존 알림 스위치 ...
            ),
            if (_systemNotificationsEnabled != false && _notify)
              ReminderEditor(
                reminders: _reminders,
                allDay: _time == null,
                onChanged: (v) => setState(() => _reminders = v),
              ),
```
(기존 `subtitle: Text(_reminderHelpText())`는 유지하거나 간단 문구로 교체 가능. `_reminderHelpText`가 더는
정확하지 않으면 "알림 시점을 선택하세요"로 바꾼다.)

`_save`의 CalendarEvent 생성에 추가:
```dart
      reminders: _reminders,
```

- [ ] **Step 2: saint_feast_editor 연결(항상 종일)**

동일하게 import + `late List<ReminderLead> _reminders;` + initState:
```dart
    _reminders = sanitizeReminders(
      e?.reminders ?? const [ReminderLead.day1],
      allDay: true,
    );
```
알림 SwitchListTile 아래:
```dart
            if (_systemNotificationsEnabled != false && _notify)
              ReminderEditor(
                reminders: _reminders,
                allDay: true, // 축일은 항상 종일 -> 일/주 리드만
                onChanged: (v) => setState(() => _reminders = v),
              ),
```
`_save`의 CalendarEvent 생성에 `reminders: _reminders,` 추가.

- [ ] **Step 3: analyze + 전체 테스트 + 커밋**

Run `flutter analyze`(무경고), `flutter test`(전체 통과; 기존 편집기 위젯 테스트 회귀 확인 - 알림 스위치 기본 on이면 ReminderEditor가 렌더되지만 기존 테스트가 특정 위젯을 find할 때 충돌 없어야 함. 충돌 시 테스트의 find를 좁힌다).
```bash
git add lib/features/events/presentation/event_editor_sheet.dart lib/features/saints/presentation/saint_feast_editor_page.dart
git commit -m "일정/축일 편집기 알림 섹션에 리드타임 선택 연결"
```

---

## Task 6: 최종 검증
- [ ] `flutter analyze` 무경고, `flutter test` 전체 통과.
- [ ] 수동(실기기/시뮬레이터, 선택): 일정 알림 on -> "1일 전" 기본, "+ 알림 추가"로 2개, 종일 전환 시 분/시간 사라짐, 축일은 일/주만. 알림 실제 수신은 기기 확인.

---

## Self-Review (작성자 점검)
- 스펙 커버리지: 8리드/혼합기준/종일처리(Task1), reminders 필드/하위호환(Task2), 스케줄러(Task3), UI 최대2/추가삭제/종일필터(Task4-5). 모두 매핑.
- 플레이스홀더: 없음(코드 포함). 단 Task1에 enum static 정의 방식은 "enum 본문에 넣는 최종형"을 명시(extension 대안은 사용 금지).
- 타입 일관성: `ReminderLead`/`reminderTime`/`reminders`/`sanitizeReminders`/`ReminderEditor` 이름 Task 전반 일관.
- 주의: initState의 `_reminders` 종일 보정은 `_time` 초기화 이후에 두어야 함(순서). notify 마스터 스위치는 그대로.
