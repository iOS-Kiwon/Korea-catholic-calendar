# 사용자 확인·작업 목록 (ACTION ITEMS)

당신이 **직접 전달/결정/검토**해야 할 항목. (⚠️ = 반드시 확인)
_최종 갱신: 2026-08-20 - 위젯 즉시 갱신에서 4x1 누락 수정 + 동기화 스로틀 추가. 실기기 검증 대기._

---

## ✅ 확정·완료된 결정 (당신이 답해주신 것)

- CBCK 데이터 이용: 별도 약관 없음 → 그대로 사용.
- 앱 이름: **"가톨릭 달력"** (앱·웹·플랫폼 전체 반영 완료).
- 번들 ID: **com.sidore.catholiccalendar** (Android `applicationId`/`namespace`/Kotlin 패키지, iOS `PRODUCT_BUNDLE_IDENTIFIER` 반영 완료).
- 주 시작 요일: 일요일 고정.
- 계정: Apple Developer / Google Play 모두 보유.

## ✅ 최근 확정·완료

- 디자인 시안(현대 절충 1c) 반영 완료.
- **앱 아이콘 적용 완료** — 전달해 주신 아이콘으로 iOS/Android/웹 아이콘 생성(`assets/icon/app_icon.png` 원본). 수정본 주시면 `dart run flutter_launcher_icons` 재실행으로 교체.
- **다크 모드 미지원 확정** — 항상 라이트 테마.
- **AdMob 배너 통합 완료** — 모든 화면 하단(SafeArea.bottom 바로 위). iOS/Android 실 ID 반영:
  - iOS 앱 `~9692360017` · 배너 `/5415157947`
  - Android 앱 `~2281981735` · 배너 `/6029655050`
  - release=실 광고, debug=테스트 광고(정책 준수). 웹은 광고 없음.
- 웹사이트 제공: 보류(결정 시 호스팅 연결 진행).

## ✅ 최근 추가 (ATT · SKAdNetwork · UMP · app-ads.txt)

- iOS **ATT** 프롬프트 + `NSUserTrackingUsageDescription`, **SKAdNetworkItems**(Google 공식 50개), **UMP 동의(GDPR)** 흐름(동의→ATT→SDK init), `web/app-ads.txt`(`pub-5980133283002959`) 코드/설정 완료.

## ✅ 최근 추가 (로컬 개인 일정 + 로컬 알림)

- **일정 추가 기능**: 화면 우하단 **플로팅 버튼(FAB, 현재 월 전례색)** → 편집 시트.
  - 필드: 날짜(필수)·제목(필수)·메모(선택)·시간(종일 또는 `HH:mm`)·알림 토글.
  - **기기 내부(`shared_preferences`)에만 저장** — 클라우드/기기 캘린더 연동 없음.
  - 하단 정보영역 요약 + 상세 "내 일정" 목록(수정/삭제) + 달력 그리드 마커(전례색 점과 구분되는 우상단 점).
- **로컬 알림(서버 없음)**: `flutter_local_notifications` + `timezone`.
  - **전날 21:00 + 당일**(시간 지정 시 그 시각, 종일은 09:00) 예약. 일정별 알림 토글(기본 켜짐).
  - iOS/Android만 동작(웹은 조건부 import 로 알림 없이 나머지 기능 정상).
  - 부정확 예약(inexact) 사용 → Android 정확 알람 특별 권한 불필요. 재부팅/앱 재설치 후 재등록(boot 리시버 + 앱 실행 시 재동기화).
- **일정 = 카테고리 선택 방식** (제목 직접 입력 없음): 사용자가 자기 카테고리를 만들고, 일정 추가 시 **카테고리를 클릭**해 등록. 메모만 부가 설명으로 직접 입력.
  - 카테고리: **이름 + 색상**(아이콘 없음).
  - **"카테고리" 화면**: 일정 편집 시트의 카테고리 칸을 탭 → 전체화면 진입. 기본은 **선택 모드**(항목 탭 → 이전 화면에 자동 설정 후 닫힘, 하단 '카테고리 추가' 버튼).
  - 앱바 **설정(⚙) 버튼** → **편집 모드**(각 항목에 이름/색 수정·삭제·이동 아이콘), 버튼은 **저장**으로 바뀌고 저장 시 현재 상태 일괄 커밋.
  - **첫 실행 시 기본 카테고리 시드**: 본당 행사·교구 행사·기도·전례·교리 (모두 편집/삭제 가능).
  - 카테고리 **이름/색 변경 → 기존 일정에도 반영**. **삭제는 사용 중이면 차단**('사용 중' 표시 + 안내).
- 브랜치: `feat/local-events` (커밋 완료). 검증: `flutter analyze` 클린, `flutter test` 29개 통과, `flutter build web` 성공.

## ✅ CBCK 캐시 게이트웨이 (배포·연동 완료)

- Cloudflare Worker 배포됨: **`https://catholic-calendar.sidore.workers.dev`** (KV 캐시 + 매일 cron 프리워밍, 월 단위·브라우저 헤더).
- 앱이 현재 보는 **달**을 게이트웨이에서 받아 사용(`monthServiceProvider`), 실패/미발행/오프라인 시 **번들 스냅샷 + 계산 엔진 폴백**.
- 검증: 2026/7=31일, 2050/1=available:false, CORS 헤더 확인.
- (선택) 나중에 커스텀 도메인 연결 시 그 도메인 루트에 `app-ads.txt`도 함께 서빙 가능.
- 참고: 게이트웨이는 호출 완화책일 뿐, 근본적으로는 **CBCK 사용 허가/제휴**가 정답.

## ✅ 최근 추가 (버그 수정: 화면 회전 잠금 + 위젯 자정 갱신)

- **화면 회전 잠금**: 세로(정방향) + 상하 반전만 지원, 가로(landscape) 미지원.
  - Flutter `SystemChrome.setPreferredOrientations`, Android `screenOrientation="sensorPortrait"`, iOS `Info.plist` 방향 배열(iPhone/iPad 모두 portrait + upside-down).
- **위젯 날짜가 자정 이후에도 전날을 가리키던 버그 수정** (근본 원인: '오늘'을 앱이 스냅샷 저장 시점에 미리 구워넣어, 위젯이 다시 그려져도 전날 데이터를 표시).
  - 이제 위젯이 **현재(엔트리) 날짜로 '오늘'을 직접 판정**하고 스냅샷 42칸 격자에서 해당 날을 찾아 그림(스냅샷 각 날에 전체 제목/날짜 라벨 추가).
  - **iOS**: 향후 8일치 '자정' 타임라인 엔트리 생성(`Calendar.autoupdatingCurrent`, `.atEnd`) → 자정마다 자동 재렌더링. 앱 실행 시 `reloadTimelines`도 유지.
  - **Android**: `AlarmManager`(부정확 예약, 특별 권한 불필요)로 매일 자정 위젯 자체 갱신 예약 + 재부팅/앱 업데이트 후 재예약(부팅 인텐트 필터). 앱 실행 시 재렌더링도 유지.
  - 한계(자연 보정): 달이 바뀌는 자정에 **큰(달력) 위젯**은 다음 앱 실행 전까지 이전 달 격자를 보일 수 있음(작은 위젯은 정상 보정).

## ✅ 최근 추가 (일정 알림 리드타임 - 사용자 지정 + 최대 2개)

- **알림 시점 사용자 지정**: 일정/축일 편집기의 알림 섹션에서 리드타임을 고른다. 옵션 8종: 5분 / 10분 / 30분 / 1시간 / 2시간 / 1일(기본) / 2일 / 1주일 전.
  - **한 일정에 알림 최대 2개** ("+ 알림 추가"로 두 번째 추가, 삭제 가능). 같은 리드 중복 선택 방지.
  - **혼합 기준**: 분/시간 단위는 일정 **시작 시각 기준**, 일/주 단위는 그 날 **아침 9시** ("1일 전 = 전날 아침 9시").
  - **종일 일정**: 분/시간 리드는 자동 숨김(일/주만). `notify` 스위치는 마스터 on/off로 유지.
  - 기존 **전날 21:00 + 당일 고정** 로직에서 변경. 기존 저장 일정은 자동으로 "1일 전" 단일 알림으로 하위호환.
- **Android 정시(exact) 알람 전환**: 짧은 리드(5분~2시간 전)가 도즈/배터리 최적화에서도 정시에 울리도록 매니페스트에 `USE_EXACT_ALARM` + `SCHEDULE_EXACT_ALARM(maxSdkVersion=32)` 선언 + `exactAllowWhileIdle` 사용. 캘린더/리마인더 앱이라 Play 정책상 자동 승인(런타임 팝업 없음). (이전 "inexact 사용, 특별 권한 불필요" 방침에서 변경)
- 브랜치: `feature/일정-반복`. 검증: `flutter analyze` 무경고, `flutter test` 164개 통과.

## ✅ 최근 추가 (일정 공유 - https 딥링크)

- 상세화면 개인 일정에 **공유 버튼** 추가 → `https://kcc.sidore.org/e/<base64url payload>` 링크 생성(날짜/시간/메모/카테고리 담김, 서버 저장 없음). 반복 일정은 앵커 날짜 오류 방지를 위해 공유 버튼 숨김.
- 앱이 이 링크를 받으면(콜드스타트/실행 중 둘 다) 해당 날짜로 이동 후 편집기를 **추가 모드**로 열어 내용 자동 입력(`resolveIncomingLink` → `ShareLinkDraft`/`ShareLinkInvalid`/`ShareLinkNeedsUpdate` 3분기). `lib/app/app.dart`에서 `app_links` 패키지로 수신, 재진입 가드 + `try/finally`로 예외 시에도 가드 해제.
- iOS 유니버설 링크(Associated Domains) / Android 앱 링크(autoVerify intent-filter) 매니페스트·entitlements 설정 완료. 서버 연동파일 미배포 상황을 위한 `catholiccalendar://` 커스텀 스킴 폴백도 등록.
- 실기기 검증 중 발견해 고친 버그 2건: (1) 딥링크로 앱이 열릴 때 go_router 기본 "Page Not Found" 화면이 잠깐 뜨던 문제(`errorBuilder` 추가로 현재 달 화면으로 대체), (2) 라우터 이동 직후 곧바로 편집기를 열면 조용히 실패해 편집기가 안 뜨던 문제(라우터 리빌드가 끝나는 한 프레임 더 기다린 뒤 열도록 수정 + 실패 시 진단 로그 추가).
- **`kcc-links`(Cloudflare Worker) 배포 완료**: `kcc.sidore.org` 커스텀 도메인 연결 및 SSL 발급 확인됨(`/health` 응답 정상). `/.well-known/apple-app-site-association`(iOS, 실제 Team ID 반영됨) / `/.well-known/assetlinks.json`(Android, SHA256은 아직 placeholder) / `/e/*` 브라우저 폴백 랜딩 모두 서빙 중.
- **공유 랜딩 페이지 디자인 개선**: 시스템 폰트 텍스트만 있던 페이지를 앱 브랜드 톤(크림 배경, 세리프 제목, 전례색 4도트, 필 버튼)으로 리디자인(B안 채택, A안은 `kcc-links/mockups/`에 보존). 설계 문서: `docs/superpowers/specs/2026-08-04-공유링크-랜딩페이지-design.md`.
- **보안 방식 결정(확정)**: URL에 담긴 payload는 암호화가 아니라 base64 인코딩 그대로 유지하기로 결정. 이유: (a) 앱에 내장해야 하는 복호화 키는 결국 누구나 추출 가능해 "진짜" 보안이 아님, (b) 암호화 시 URL이 더 길어짐(방향 반대), (c) 개인 일정 공유 용도로는 현재 방식(서버 미저장, 링크를 아는 사람만 열람 가능)이 충분. 링크 취소가 안 되고 메모에 민감 정보는 넣지 말아야 한다는 한계는 인지하고 유지.
- 검증: `flutter analyze` 무경고, `flutter test` 181개 통과(신규 케이스 포함). 링크 처리 핵심 분기(`resolveIncomingLink`)는 단위 테스트로 커버.

## ✅ 최근 추가 (앱스토어 리뷰 요청 - 구현 완료, 배포 전)

OS 네이티브 리뷰창(iOS `SKStoreReviewController` / Android In-App Review)을 붙였습니다. 설계 문서 `docs/superpowers/specs/2026-08-10-앱스토어-리뷰요청-design.md`, 구현 계획 `docs/superpowers/plans/2026-08-10-앱스토어-리뷰요청.md`. 브랜치 `feature/리뷰`.

- **확정된 조건**: **저장된 일정 5개 이상**, 그것 하나뿐입니다. 설치 후 경과일도, 앱 실행 횟수도 보지 않습니다. 일정 5개를 채우는 것 자체가 앱을 실제로 쓰고 있다는 신호라고 봤습니다. (여기에 서버 킬스위치 켜짐 + 아직 요청한 적 없음이 함께 걸립니다)
- **노출 시점**: **일정을 저장한 직후, 오직 이때만.** 5번째 일정을 저장하는 순간 뜹니다. 앱 시작 시에는 띄우지 않습니다. "앱을 열어보기만 한 사용자에게 리뷰를 청하는 건 과하다"는 결정에 따른 것입니다.
- **횟수**: 설치당 **1회**. 요청하고 나면 그 설치에서는 다시 요청하지 않습니다.
- **리뷰 작성 여부는 구분하지 않습니다** - 애초에 앱이 알 수 없습니다. 사용자가 리뷰를 썼는지는 OS만 알고 앱에는 알려주지 않습니다. 조건을 만족하면 작성 이력과 무관하게 1회 요청하고, 이미 쓴 사람에게 안 띄우는 판단은 시스템에 맡깁니다.
- **재설치하면 다시 노출됩니다(조건부)** - 리뷰 이력은 `shared_preferences`에 있어 앱을 지우면 사라지므로, 재설치 후 일정 5개를 다시 채우면 한 번 더 요청합니다. 단 **백업 복원으로 재설치한 경우는 예외**입니다. Android는 `allowBackup` 기본값이 `true`라 Auto Backup이 켜져 있고, 같은 계정으로 재설치하면 리뷰 이력과 일정 데이터가 **함께** 복원됩니다(iOS도 iCloud 복원 시 동일). 이 경우 "이어서 쓰는 같은 사용자"이므로 다시 요청하지 않는 게 자연스러워 그대로 뒀습니다.
- ⚠️ **효과 측정은 간접적입니다.** iOS/Android 모두 "리뷰창이 실제로 떴는지, 사용자가 별점을 매겼는지"를 앱에 알려주지 않습니다. Firebase의 `app_review_requested`는 **요청 시도 수**일 뿐이고, 실제 효과는 스토어 콘솔의 리뷰 수 추이로 봐야 합니다.
- **시스템 쿼터가 있습니다** - iOS는 365일 3회, Android는 비공개 쿼터. 초과분은 시스템이 조용히 삼키고 앱은 그 사실을 알 수 없습니다. 그래서 서버 킬스위치를 둡니다.
- **코드 수정 범위**: 조건이 단순해지면서 `lib/app/app.dart`와 기존 시작 팝업들(백업 알림·업데이트 다이얼로그)은 **전혀 건드리지 않았습니다.** 신규 `lib/features/app_review/`(판정 + 서비스 2파일), 수정은 `pubspec.yaml`(`in_app_review` 추가) / `app_metadata_service.dart`(킬스위치) / `event_editor_sheet.dart`(연결) 3개가 전부입니다.
- **검증**: `flutter analyze` 무경고, `flutter test` 202개 통과(신규 18개 포함 - 판정 8, 서비스 6, 킬스위치 4). 기존 위젯 테스트가 이미 "카테고리 선택 → 저장"까지 가는 경로라 새 코드 경로도 함께 밟고 통과했습니다.

## ✅ 최근 추가 (주간 위젯 1x4 - Android 전용)

- **이번 주 7일을 가로 한 줄로 보여주는 위젯**을 추가했습니다. 4x4 월간 달력의 한 행만 떼어낸 형태로, 칸 구성(날짜 숫자 + 일정/전례일 텍스트 + 오늘 주황 배경)은 4x4와 동일합니다. 위젯 목록 이름은 **"주간 전례력 4x1"**.
- ⚠️ **Android 전용입니다.** iOS(WidgetKit)는 홈 화면 위젯 크기가 systemSmall(2x2)/systemMedium(4x2)/systemLarge(4x4) 세 가지로 고정되어 있어 **4x1이라는 크기 자체가 존재하지 않습니다.** 크기를 임의로 정할 수 없어 구현이 불가능하며, iOS 코드는 건드리지 않았습니다. 플랫폼별로 제공 위젯 종류가 달라집니다.
- **주 이동 버튼은 없습니다.** 항상 이번 주를 보여주고 탭하면 앱이 열립니다. 1x4 높이(약 62dp)에 4x4의 `‹ 오늘 ›` 헤더(36dp)를 넣을 공간이 없고, 달력을 넘겨 보는 용도는 4x4 위젯이 담당합니다.
- **달이 바뀌어도 정확합니다.** 오늘 날짜로 달을 계산해 스냅샷의 ±12개월 격자에서 주를 찾으므로, 앱을 열지 않아도 최대 1년간 올바른 주를 그립니다. (4x4에 있던 "달이 바뀌는 자정에 이전 달 격자를 보이는" 한계를 주간 위젯은 겪지 않습니다)
- **달 경계 주 처리**: 주가 두 달에 걸치면(예: 8/31~9/6) 다음 달 날짜도 **진하게 + 전례일 이름/전례색과 함께** 나오도록 각 칸을 그 날이 속한 달 격자에서 다시 찾아 보정합니다.
- **Dart 코드는 변경하지 않았습니다.** 스냅샷에 필요한 데이터가 이미 다 있어 Android 리소스 + Kotlin만 손봤습니다. 기존 2x2/4x4 위젯의 동작·외형도 그대로입니다.
- 설계 `docs/superpowers/specs/2026-08-13-주간-위젯-1x4-design.md`, 계획 `docs/superpowers/plans/2026-08-13-주간-위젯-1x4.md`. 브랜치 `feature/위젯_1_4`.
- 검증: `flutter build apk --debug` 성공, `flutter analyze` 무경고, `flutter test` 184개 통과(회귀 확인용 - 이번 변경은 Dart를 건드리지 않음).

## ✅ 최근 추가 (위젯 즉시 갱신 - 4x1 누락 수정 + 스로틀)

- **원래도 즉시 갱신은 되고 있었다.** `app.dart`의 `ref.listen(eventStoreProvider, ...)` → `WidgetSnapshotService.sync` → 네이티브에서 곧바로 `updateAppWidget`. 일정·축일 추가/수정/삭제와 카테고리 이름·색 변경 모두 `EventStore._persistAndSync`가 `state = AsyncData(map)`를 하므로 발동한다. iOS도 App Group 저장 + `WidgetCenter.reloadTimelines`까지 정상(위젯 kind 1개).
- **수정한 버그**: `MainActivity.syncWidgetSnapshot`이 provider 목록을 따로 하드코딩해 **2x2/4x4만** 갱신했다. 나중에 추가된 **4x1 주간 위젯이 빠져** 일정을 추가/삭제해도 다음 자정이나 재부팅까지 예전 내용을 보여줬다. → `TodayWidgetProvider.refreshAllWidgets`(3개 전부 갱신, 자정·부팅 경로가 쓰던 함수)를 공개해 `MainActivity`가 호출하도록 바꿨다. 목록은 이제 한 곳에만 있다.
- **추가한 스로틀** (`lib/features/widgets/widget_sync_throttle.dart`): 첫 요청은 즉시, 쿨다운(800ms) 동안 몰린 요청은 마지막에 1회. 흔한 트레일링 디바운스를 쓰지 않은 이유 - 저장 직후 홈으로 나가면 타이머가 못 돌아 위젯이 안 바뀔 수 있다. 요청을 유발한 프레임은 막지 않도록 `addPostFrameCallback`으로 넘긴다.
- **왜 필요했나 (측정값)**: 스냅샷은 매번 ±12개월 × 42일 = 1050일 + 현재월을 다시 만들어 400KB 넘는 JSON을 뽑는다.

  | 일정 수 | 페이로드 | `sync()` (macOS 테스트 VM) |
  |---|---|---|
  | 0건 | 428 KB | 53 ms |
  | 10건 | 429 KB | 73 ms |
  | 50건 | 433 KB | 144 ms |
  | 200건 | 451 KB | 378 ms |

  고정비 약 53ms + 일정당 약 1.6ms. 비용 대부분은 `RecurrenceExpander.eventsOn`을 1092일 × 전체 일정에 대해 돌리는 것(200건일 때 이것만 414ms). 실기기는 2~4배 느릴 수 있다. `ensureMonth`가 원격 병합마다 `state = AsyncData(service)`를 하고 `preloadAround`가 3개월을 병렬 로드하므로 앱 시작·월 이동 때 **연달아 최대 4번** 발동했다 → 스로틀로 2번(즉시 1 + 트레일링 1)으로 줄었다.
- 테스트 6건 추가(스로틀 4건 + 4x4 월 이동 데이터 1건 + 기존). `flutter test` 261건 통과, `flutter analyze` 경고 0, `:app:compileDebugKotlin` BUILD SUCCESSFUL.

### 4x4 월 이동: 기본 표시 월 + 범위 밖 안내 화면

- **기본 표시 월을 기기 시계의 현재 달로** 바꿨다(`displayedMonthSerial`). 예전 기본값은 `snapshot.month`(앱을 마지막 실행한 달)여서, 앱을 한 달 넘게 안 열면 지난달이 남고 **`오늘` 버튼을 눌러도 지난달로 갔다**(오늘 버튼은 저장값을 지워 기본값으로 돌아가는 방식). 스냅샷은 Flutter 없이 재생성할 수 없어 자정 갱신으로도 안 바뀐다.
- **범위 밖으로 나가면 격자 대신 안내를 보여준다.** 위젯은 RemoteViews라 Flutter를 호출할 수 없어 스냅샷에 구워진 ±12개월 밖은 그릴 수 없다.
  - `handleWidgetAction`에서 serial을 `[min-1, max+1]`로 clamp → 범위 밖은 정확히 한 칸이고, 반대 화살표 한 번으로 달력에 복귀. clamp가 없던 예전에는 serial이 무한히 커져서 `다음`을 30번 누르면 `이전`을 31번 눌러야 화면이 바뀌었다(버튼이 고장난 것처럼 보임).
  - 헤더(제목/‹/›/오늘)는 그대로 두고 요일 행 + 격자만 숨긴다 → 돌아갈 길이 항상 있다.
  - 문구는 방향에 따라 `"이후/이전 일정은 앱에서 확인하세요."`, 버튼은 `[앱으로 이동하기]`. 스냅샷이 아예 없는 첫 실행에는 `"앱을 한 번 실행하면 달력이 표시됩니다."`(예전에는 빈 격자만 나왔다).
  - 제목은 `monthTitleOf(serial)`로 이동한 달을 그대로 보여준다(예: `2027.9`) → 격자가 없는 이유가 설명된다.
- **부수 효과: 스냅샷 범위를 줄일 수 있게 됐다.** 지금까지 ±12개월을 굽는 이유가 "범위를 벗어나면 화면이 깨져서"였는데, 이제 안내 화면이 받아준다. ±2~3개월로 줄이면 페이로드 428KB → 약 90KB, 고정비 53ms → 약 15ms.
- **자동 테스트로 잡을 수 없다** (RemoteViews, Kotlin 단위 테스트 없음). 실기기 확인 필요.

### 위젯 공휴일·대체공휴일 빨간색 (iOS/Android)

- 스냅샷 `_dayPayload`와 `today`에 `isHoliday`(= `CalendarService.isKoreanHoliday`)를 추가하고, Android `dayNumberColor` / iOS `MonthDayCell.numberColor`가 이 플래그로 날짜 숫자를 **주일과 같은 빨간색**으로 그린다.
- 우선순위는 앱 달력(`day_cell.dart`의 `_numberColor`)과 같게 맞췄다: 오늘 → 다른 달 → **공휴일** → 주일 → 토요일.
- **대체공휴일은 자동으로 포함**된다. `isKoreanHoliday`가 날짜 키 조회일 뿐이고 `korean_holidays.snapshot.1900-2100.json`에 대체공휴일이 별도 항목으로 들어 있다(2026년: 3/2, 5/25, 8/17, 10/5).
- iOS `DaySnapshot.isHoliday`는 **optional**이다. 구버전 스냅샷(필드 없음)으로도 디코딩이 깨지지 않아야 한다 - `titleFull`/`dateLabel`과 같은 이유.
- 테스트: 광복절(2026-08-15) · 대체공휴일(2026-08-17)은 true, 평일(8/18)과 주일(8/16)은 false.

### 위젯 날짜 탭 → 앱의 해당 날짜 (신규)

- 날짜 칸을 누르면 그 날짜가 선택된 앱 메인 화면이 열린다. 범위 밖 안내의 `[앱으로 이동하기]`는 그 달의 달력을 연다.
- **커스텀 스킴 딥링크를 쓴다** - `catholiccalendar://day/2026-08-15`, `catholiccalendar://month/2027-09`. 공유 링크(`host == 'e'`)와 같은 스킴이고 host로 갈린다. `app_links`가 받아 `resolveWidgetLink`(`lib/features/widgets/widget_deep_link.dart`)가 해석한다.
  - MethodChannel + 인텐트 extra 대신 이 방식을 고른 이유: ① 공유 링크에서 이미 검증된 경로라 콜드/웜 스타트를 둘 다 공짜로 처리한다(`launchMode=singleTop` + `getInitialLink` + `uriLinkStream`) ② **iOS 위젯에도 그대로 쓸 수 있다** - iOS는 셀을 `Link`로 감싸기만 하면 Dart 배관을 재사용한다.
  - `AndroidManifest.xml`에 `host="day"`/`host="month"` intent-filter 추가.
- **PendingIntent 함정**: PendingIntent는 extras를 무시하고 action/data/component로 동일성을 판정한다. 42칸이 같은 URI를 쓰면 하나로 합쳐져 **모든 칸이 같은 날짜를 연다.** data URI를 칸마다 다르게 주고 requestCode도 `yyyyMMdd`로 분리했다(달은 음수 serial).
- 앱 쪽은 `_openWidgetTarget`에서 `popUntil(isFirst)` 후 `_router.go(location)`. 설정/상세가 위에 쌓여 있으면 걷어내지 않으면 달력만 뒤에서 바뀌어 사용자가 변화를 못 본다.
- **새 달력 화면이 쌓이던 문제 수정**: `router.dart`에서 `:day`가 `/:year/:month`의 **자식** 라우트였다. go_router는 자식 라우트를 스택으로 쌓으므로 `/2026/08` → `/2026/08/15` 이동이 달력 페이지를 하나 더 올려, 위젯에서 날짜를 누르면 새 화면이 열린 것처럼 보였다. → **형제 라우트로 평탄화** + `pageBuilder`에서 **달 단위 페이지 키**(`ValueKey('calendar-2026-8')`)를 준다. 같은 달 안에서는 키가 같아 Navigator가 같은 페이지를 그 자리에서 다시 빌드하고(전환 애니메이션도 없다), 선택은 `CalendarPage.didUpdateWidget`이 받아 적용한다. 다른 달이면 키가 달라져 새 페이지가 되고 `initState`가 적용한다. 테스트 4건으로 "달력 페이지가 항상 1개"를 못 박았다.
- 잘못된 입력 방어: 스냅샷이 오래돼 존재하지 않는 날짜(예: `2026-02-30`)가 올 수 있어 달·일 범위를 모두 검사한다. 테스트 11건.
- **iOS도 같이 적용**(`ios/TodayWidget/TodayWidget.swift`). 격자 칸을 `Link(destination:)`으로 감쌌고, `widgetURL`은 `catholiccalendar://today`(앱이 해석하지 않아 기본 화면만 열렸다) → `catholiccalendar://day/<오늘>`로 바꿨다. **Dart·URL 형식은 Android와 완전히 공유**하므로 Swift만 손댔다. 스킴은 Info.plist에 이미 등록돼 있어 plist 변경 없음. `xcrun swiftc -typecheck -parse-as-library` 통과(CocoaPods가 없어 전체 iOS 빌드는 못 함).

## ✅ 최근 추가 (기념/전례 행을 전례색 배지로 통일)

- **증상**: 8/19 성 요한 외드 사제의 `축일` 칩에 배경색이 없어 보임.
- **원인**: `축일` 칩은 전례색을 24% 알파 배경으로만 깔았는데, 백색 토큰(`#F8F9FA`)을 카드 표면(`#F8F6F0`) 위에 24%로 얹으면 합성 결과가 `#F8F7F2` - 채널당 0~2 차이라 사실상 같은 색이었다. `LiturgicalColorLabel`에는 백색 전용 보정(`#666666` 배경 + 흰 글자, 커밋 `e8f8db9`)이 있었지만 `EventLabelHighlight` 직접 호출 경로에는 반영되지 않았다.
- **결정** (당신이 선택): 종류 칩(`축일`/`전례`)을 없애고 **두 행 모두 전례색 배지(`[녹]`/`[백]`/`[홍]`)로 통일**. 하단 정보 카드 + 날짜 상세 양쪽 적용.
  - 근거: 색은 그 자체로 정보다. 선택 기념일 200건 중 **백색 151 / 홍색 49**(홍색 = 순교자·사도)이고, 그날 색과 기념일 색이 다른 경우가 압도적이다(녹+백 122, 녹+홍 35, 백+홍 14, 자+백 11 / 같은 색 18). 즉 "기념하기로 하면 제의색이 그날 기본색과 달라진다"는 실용 정보를 담는다.
  - **의도적으로 없앤 것**: 칩의 `축일`/`전례` 종류 표시. 나중에 버그로 오인해 되살리지 않도록 기록한다. `축일 추가` 버튼은 여전히 `displayType`으로 결정되므로(`_canAddSaintFeast`) 종류 정보 자체는 데이터·기능에 살아 있다.
  - 함께 제거된 죽은 코드: `day_info_bar.dart`의 `_memorialTypeLabel` / `_isLiturgicalOnlyMemorial` / `_isSaintFeastMemorial` / `_looksLikeSaintTitle`, `day_detail_view.dart`의 `_displayTypeLabel` / `_fallbackLiturgicalLabel`, `_MemorialLine`의 `id`/`rank`/`displayType` 필드.
- 테스트: 칩 라벨을 검증하던 4건을 새 동작으로 재작성. 그중 하나는 **신고된 8/19 케이스 회귀 테스트**로 바꿔, `[백]` 배지의 배경이 `#666666` 92%(보이는 값)이고 거의 흰색이 아님을 직접 단정한다. 이름이 실제 동작과 맞지 않게 된 4건은 이름만 정리. `flutter test` 255건 통과, `flutter analyze` 경고 0.

## ✅ 최근 추가 (OS 글꼴 확대 / 굵은 글씨 - 달력 grid 전례명)

- **원인**: `TextOverflow.ellipsis`는 `maxLines` 초과에만 `…`를 넣고 **박스 높이 초과는 예외도 경고도 없이 잘라낸다**(Flutter `rendering/paragraph.dart:949`). 전례명 3줄 슬롯의 여유가 폰 2px·와이드 0.6px뿐이라 배율 1.07(폰)/1.02(와이드)부터 3번째 줄이 반쯤 잘렸다. `maxLines` 결정이 행 높이 임계값(70/59)을 배율 1.0 전제로 하드코딩한 것이 원인.
- **결정한 정책** (당신이 선택):
  1. **줄 수 자동 강등** - 남은 높이를 실측해 3줄 → 2줄 → 1줄로 줄이고 `…`로 끝냄. 행 높이 70/93, 그리드 420/558, 6줄 고정은 **그대로**.
  2. **굵은 글씨는 grid 라벨에서만 무효화** - 라벨은 이미 `w600`. 날짜 숫자·요일·헤더·하단 카드·상세 화면은 굵게 적용 유지.
  3. **범위는 전례명 라벨만** (아래 "남은 당신의 몫" 참조).
- 라벨 전용 배율 상한 `1.5` 추가 (`kGridLabelMaxTextScale`). 상한 없이 배율 2.0이면 라벨에 글자 2~3개만 들어가 오히려 정보가 줄어든다. 같은 전례명이 하단 정보 카드/상세 화면에서 상한 없이 제공되므로 WCAG 1.4.4 위반이 아니다. **불필요하면 이 상수를 `double.infinity`로 바꾸면 무효화**된다.
- **배율 1.0 픽셀 무변화 확인** - 라벨 기하가 수정 전과 완전히 동일(`size=(51.4, 32.0)`, `textSize=(51.4, 30.0)`, `maxLines=3`, `topLeft=(374.3, 303.0)`). 회귀 테스트로 고정해 뒀다.
- 회귀 테스트 51건 추가(`test/calendar_label_scale_test.dart` + `test/widget_test.dart`). 배율 1.0~3.0 × 굵게 on/off × 플랫폼 줄간격 오버라이드 × Android 14 비선형 스케일러. **수정 전 코드에서 45건이 실패하는 것을 먼저 확인**했다. `flutter test` 255건 전부 통과, `flutter analyze` 경고 0.

## ⏳ 남은 당신의 몫

- [ ] ⚠️ **위젯 즉시 갱신 실기기 검증** - 4x1 주간 위젯을 홈에 두고 앱에서 일정/축일을 추가·삭제했을 때 **곧바로** 바뀌는지 확인해 주세요(이전에는 안 바뀌었습니다). 2x2/4x4도 회귀 확인.
- [ ] **위젯 안내 화면 문구·디자인 확인** - 4x4에서 스냅샷 범위 밖으로 이동하면 격자 대신 안내가 나옵니다(아래 "최근 추가" 참고). 문구는 `"이후/이전 일정은 \n앱에서 확인하세요."`, 버튼은 `[앱으로 이동하기]`(녹색 캡슐)입니다. 실기기에서 보시고 문구·크기·색을 조정할지 알려주세요.
- [ ] ⚠️⚠️ **위젯 날짜 탭이 안 먹는 원인 확정 (미해결)** - 날짜를 눌러도 오늘만 선택된다는 보고. **재현하지 못했습니다.** Dart 경로(`/2026/08/15` → 15일 선택)는 자동 테스트로 정상 확인됐고, `app_links`는 `intent.getDataString()`을 그대로 쓰므로 data가 있는 인텐트면 반드시 전달됩니다. 즉 **data 없는 인텐트가 발동한 것**으로 보입니다. 아래 순서로 원인을 가려 주세요:
  1. **위젯을 홈에서 제거하고 다시 추가**해 보세요. 런처는 RemoteViews를 캐시하므로, 재설치만 하고 위젯이 다시 그려지지 않았으면 **클릭 핸들러가 없던 예전 RemoteViews가 그대로 남아 있습니다.** 이게 가장 흔한 원인입니다.
  2. 그래도 안 되면 **4x4에서 날짜를 눌렀을 때 무엇이 열리는지** 보세요. 이번에 위젯 배경 탭도 딥링크를 싣도록 바꿨기 때문에 구분이 됩니다 - **"선택 없는 그 달"이 열리면 날짜 칸 대신 배경 클릭이 먹은 것**이고, 엉뚱한 달·오늘이 열리면 링크 전달 문제입니다.
  3. 링크 경로만 따로 시험: `~/Library/Android/sdk/platform-tools/adb shell am start -a android.intent.action.VIEW -d "catholiccalendar://day/2026-08-15"` → 15일이 선택되면 매니페스트·app_links·라우팅은 정상이고 위젯 클릭만 문제입니다.
  4. `flutter logs`(또는 logcat)에 `[KCC link] 수신 uri=... → widgetTarget=...` 로그를 이번에 추가했습니다. 이 줄이 아예 없으면 링크가 안 온 것, `widgetTarget=null`이면 해석 실패입니다.
- [ ] ⚠️ **위젯 날짜 탭 나머지 검증** (위가 해결된 뒤)
  - **여러 날짜를 각각 눌러 서로 다른 날짜가 열리는지** (PendingIntent가 하나로 합쳐지는 고전적 버그. data URI를 칸마다 다르게 줘서 막았습니다)
  - 앱이 꺼진 상태(콜드)와 실행 중(웜) 양쪽
  - 앱에서 설정/상세 화면에 들어가 있는 상태에서 위젯 날짜를 누르면 그 화면들이 걷히고 달력이 보이는지
  - 범위 밖 안내의 `[앱으로 이동하기]`가 그 달의 달력을 여는지
- [ ] ⚠️ **iOS 위젯 날짜 탭 실기기 검증** - `xcrun swiftc -typecheck`는 통과했지만 **CocoaPods가 없어 실제 iOS 빌드는 못 했습니다**(`sudo gem install cocoapods` 필요). 확인할 것:
  - systemLarge 격자에서 날짜를 눌러 그 날짜가 선택된 앱 화면이 열리는지, **칸마다 다른 날짜가 열리는지**
  - 제목·요일 행을 누르면 오늘로 열리는지(`widgetURL`)
  - systemSmall/Medium 전체 탭이 오늘로 열리는지
  - `Link`의 tint가 셀 글자색을 물들이지 않는지(모든 Text에 색을 명시해 뒀지만 눈으로 확인)
- [ ] (선택) **스냅샷 생성 비용 줄이기** - 위 표 참고. 효과 순서: ① `RecurrenceExpander`를 날짜별 순회 → 일정별 발생일 전개로 뒤집기(200건에서 378ms → 약 60ms 예상) ② 페이로드 범위 ±12개월 → ±2~3개월(고정비 53ms → 약 15ms, 428KB → 약 90KB). ②는 위 4x4 월 이동 결정과 함께 판단해야 합니다.
- [ ] **전례색 범례를 폰에도 노출할지 결정** - 이제 하단 카드·상세의 기념 행이 전례색 배지(`[녹]`/`[백]`/`[홍]`)만으로 종류를 전달합니다. 그런데 색 체계를 설명하는 `Legend`(`legend.dart`: 연중=녹 / 축일·성인=백 / 사도·순교=홍 / 사순·대림=자)는 **와이드(웹/태블릿) 레이아웃에만** 있습니다(`calendar_page.dart`의 `_wide()`에만 있고 `_narrow()`에는 없음). 폰 사용자는 범례를 본 적이 없어 `[홍]`이 순교자를 뜻하는지 모릅니다. 설정 화면이나 상세 화면 하단에 한 줄 범례를 넣는 것을 검토해 주세요.
- [ ] (선택) **홍색이 공휴일 빨강과 같은 값** - 전례색 홍색(`liturgical_colors.dart:30`)과 공휴일·주일 숫자 색(`day_cell.dart:19-20`)이 둘 다 `0xFFC62828`로 완전히 같습니다. `[홍]` 배지가 "빨간 날"로 오독될 수 있어, 구분이 필요하면 한쪽 톤을 조정해야 합니다.
- [ ] ⚠️ **OS 글꼴 확대/굵은 글씨 실기기 검증** - 설정 → 디스플레이 → 글꼴 크기 최대 + 굵은 텍스트 ON 상태에서:
  - 전례명이 2줄(또는 1줄) + `…`로 끝나고 **3번째 줄 반쪽이 없는지**
  - 날짜를 탭 → 하단 정보 카드와 상세 화면에서 **큰 글씨 + 굵게가 유지되는지**(범위 격리 확인)
  - 글꼴 크기를 기본으로 되돌렸을 때 화면이 **이전과 똑같은지**
  - 6줄 월(2026-05, 2026-08)과 5줄 월, 월 스와이프 전환 시 깜빡임/재배치 없는지
  - Android 14+ 기기라면 "글꼴 크기"와 "표시 크기"를 **따로** 올려 비선형 스케일 경로도 확인
- [ ] ⚠️ **범위 밖으로 남긴 잘림 지점 - 후속 진행 여부 결정** (전례명만 핫픽스하기로 했으므로 손대지 않음. 우선순위 순):
  1. **날짜 숫자 (가장 시급 - 배율 1.0에서도 이미 잘리는 중)** - `DayNumber`가 `bodyLarge`의 M3 `height: 1.50`을 물려받아 18px 글자가 27px 줄 박스를 요구하는데, 와이드 레이아웃 선택 셀의 원 지름은 24px이다. 테스트로 실측 확인했다(`"1" box=24.0 < text=27.0`). `calendar_page.dart:244`가 항상 한 칸을 선택 상태로 만들기 때문에 웹/태블릿에서는 매 화면 한 셀이 이 상태다. 숫자에 디센더가 없어 눈에 잘 안 띄지만, 배율이 오르면 `'25'`가 `'2'`/`'5'`로 줄바꿈된 뒤 잘린다. 최소 수정은 `height: 1.0` + `maxLines: 1, softWrap: false`.
  2. **요일 행** - `_weekdayRowHeight = 36.0` 안에서 내용이 약 34.9px → 배율 1.05부터 넘침. `SizedBox` → `ConstrainedBox(minHeight: 36)`이면 배율 1.0에서 픽셀 동일하면서 자란다.
  3. **월 헤더** - `month_header.dart:66` `SizedBox(height: 44)`. 제목에 `maxLines: 1, overflow: ellipsis`를 넣는 2줄 변경만으로도 클립이 사라진다. 별건으로 `_chevron`의 `44×44`는 Material 최소 터치 타깃 48dp 미달이며 Play 사전 출시 보고서의 Accessibility Scanner가 잡는 항목이다(폰트 배율과 무관하게 이미 경고 대상).
  4. **하단 정보 카드의 `_fixedLineHeight`** - 논리 픽셀 목표에서 `height` 배수를 역산하는 패턴. 배율 1.0에서만 24px이 되고, 비선형 스케일링에서 24:20 리듬 비가 깨지며, 무엇보다 **플랫폼 줄간격 오버라이드가 이 값을 그냥 삭제한다**. 수학적으로 구제 불가한 패턴이라 줄 높이는 M3 기본값에 맡기고 리듬을 `SizedBox` 간격으로 옮기는 것이 유일한 해법.
  5. **아이콘이 배율을 따라가지 않음** - `applyTextScaling`의 전역 기본값이 `false`(Flutter `widgets/icon.dart:267`). 배율 2.0에서 텍스트는 2배가 되는데 헤더 화살표와 하단 카드 셰브런은 그대로 남는다.
  6. **전역 배율 상한 없음** - 설정/상세/에디터 등 다른 화면은 이번에 검증하지 않았다. 도입한다면 `2.0`이 근거 있는 값(Android 설정 UI 최대치 = WCAG 1.4.4 경계). 그보다 낮으면 1.4.4 위반, 없으면 `adb shell settings put system font_scale 3.0` 같은 비정상 값에 노출.
  7. **안드로이드 홈 위젯** - RemoteViews/Kotlin이라 툴체인이 완전히 다르고 공유 코드가 0. `today_widget_day_cell.xml`의 `layout_height="16dp"` 고정 + `13sp`/`7.5sp`, `TodayWidgetProvider.kt`의 `COMPLEX_UNIT_SP`가 시스템 배율에 무제한 노출되어 같은 종류의 클립이 발생한다. 위젯은 셀 크기가 런처 고정이고 스크롤·강등 UI가 없어 `fontScale` 보정 외에 선택지가 없다.
- [ ] ⚠️ **주간 위젯 실기기 검증** - 이 기능은 **자동 테스트로 잡을 수 있는 게 없습니다**(Kotlin 단위 테스트가 없고 Dart는 미변경). 눈으로 확인해 주세요:
  - 위젯 목록에 "주간 전례력 4x1"이 뜨고 미리보기가 보이는지, 홈 화면에 배치되는지
  - 이번 주 7일이 요일 헤더와 열이 맞는지, **오늘 칸에 연한 주황 배경**이 오는지
  - **달 경계 주(핵심)**: 기기 날짜를 달이 바뀌는 주(예: 8/31 포함 주)로 맞추고 다음 달 날짜가 진하게 + 전례일 이름/전례색과 함께 나오는지
  - 자정에 오늘 표시가 옮겨가는지, **토요일 → 일요일 자정에 다음 주로 넘어가는지**
  - 기존 2x2 / 4x4 위젯이 그대로인지(회귀)
- [ ] **4x4 본문 줄 수 실측 후 결정** - 같은 기기에서 4x1과 4x4가 본문을 각각 몇 줄 보여주는지 세어봐 주세요. 4x1은 계산상 2줄이 나오지만 4x4는 최악의 경우 1.3줄입니다. **다만 `minHeight`는 배치 하한선일 뿐 실제 위젯 높이가 아니라, 기기에 따라 4x4도 이미 2줄일 수 있습니다.** 이미 2줄이면 손댈 필요가 없고, 1줄이면 그때 조절 범위를 정하면 됩니다(설계 문서의 "보류: 4x4 본문 2줄화" 참고 - 셀 패딩·요일 헤더·상단 헤더를 다 줄여야 겨우 2줄이라 기존 위젯 외형이 바뀝니다).
- [ ] **공유 랜딩 페이지 재배포** - `kcc-links/src/index.js`를 리디자인했지만 Cloudflare 대시보드에 다시 배포해야 실제 `kcc.sidore.org`에 반영됩니다. Worker 편집 화면(Edit code)에서 `kcc-links/src/index.js` 전체 내용을 다시 붙여넣고 배포해 주세요.
- [ ] **Android 앱 링크용 SHA256 지문 채우기** - `kcc-links/src/index.js`의 `ANDROID_SHA256` 두 값(`<SHA256_DEBUG>`, `<SHA256_RELEASE>`)이 아직 placeholder라 안드로이드에서 "자동으로 앱 열기"(앱링크 검증)까지는 안 됩니다(랜딩 페이지는 정상 동작). 서명 키스토어 비밀번호가 필요한 명령이라 대신 실행하지 않았습니다:
  - 디버그: `keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android -keypass android` 출력의 `SHA256:` 값
  - 릴리스: `keytool -list -v -keystore android/app/upload-keystore.jks -alias upload` 실행(비밀번호는 `android/key.properties` 참고) 후 `SHA256:` 값
  - 값을 채운 뒤 위 재배포 절차와 함께 반영.
- [ ] **실기기 최종 확인** - iOS는 카카오톡이 아닌 iMessage/메모 앱에 링크를 붙여넣어 눌러보는 것이 유니버설 링크 동작을 가장 깨끗하게 확인하는 방법입니다(카카오톡 인앱 브라우저는 OS의 자동 앱 전환을 막는 경우가 있어 랜딩 페이지가 먼저 뜨는 게 정상일 수 있음). Android도 SHA256 반영 후 링크로 앱이 바로 열리는지 확인.
- [ ] **AdMob 콘솔에서 동의/개인정보 메시지 생성** — AdMob → 개인정보 보호 및 메시지(Privacy & messaging)에서 **GDPR(유럽) 동의 메시지**(및 원하면 IDFA/ATT 사전 설명 메시지)를 만들어 게시해야 UMP 동의 폼이 실제로 표시됩니다. (코드는 준비됨)
- [ ] **`app-ads.txt` 호스팅** — App Store/Play 개발자 프로필에 등록한 **웹사이트 도메인 루트**(`https://<도메인>/app-ads.txt`)에 올려야 인증됩니다. 웹 배포 도메인이 정해지면 `web/app-ads.txt`가 자동 서빙되거나, 해당 도메인 루트에 파일을 두세요.
- [ ] **AdMob 실기기 검증** — 배너 노출 + (EEA 시뮬레이션 시) 동의 폼 + iOS ATT 프롬프트 확인. (iOS는 CocoaPods 필요: `sudo gem install cocoapods`)
- [ ] (선택) Android **적응형 아이콘(adaptive)** 전경/배경 이미지.
- [ ] 출시 전 **AdMob 정책 검토**.
- [ ] **일정 알림 실기기 검증** - 실제 iOS/Android 기기에서: (1) 첫 일정 추가 시 알림 권한 허용, (2) 리드타임을 골라(예: 5분 전) 가까운 시각 일정을 만들어 **선택한 시점에 알림 수신** 확인, (3) 알림 2개 추가 시 둘 다 수신. 특히 **Android 짧은 리드(5분~2시간 전)가 도즈 상태에서도 정시에 오는지**(exact 알람) 확인. (시뮬레이터/에뮬레이터에서도 예약 시각을 몇 분 뒤로 잡아 테스트 가능)
- [ ] ⚠️ **Play Console USE_EXACT_ALARM 정책** - Android 정시 알람용 `USE_EXACT_ALARM` 권한은 출시 심사 시 "핵심 기능이 알람/캘린더/리마인더"임을 근거로 자동 승인 대상이나, Play Console에서 권한 선언/설명을 요구할 수 있음(캘린더 알림 앱임을 명시). 반려 시 fallback으로 inexact 되돌리기 가능.
- [ ] (선택) 알림 아이콘 — 현재 런처 아이콘(`@mipmap/ic_launcher`) 사용. Android 상태바에서 더 또렷하게 하려면 흰색 실루엣 전용 아이콘을 주면 교체 가능.
- [ ] (선택) `feat/local-events` 브랜치를 `master`로 병합할지 결정(원하면 PR 생성).
- [ ] **화면 회전 실기기 검증** — Android/iOS에서 기기를 가로로 돌려도 앱이 회전하지 않고 세로 유지되는지 확인.
- [ ] **위젯 자정 갱신 실기기 검증** — 홈 화면에 위젯을 두고 (1) 자정 이후 오늘 날짜/전례가 갱신되는지, (2) 큰 위젯의 '오늘' 하이라이트가 이동하는지 확인. (에뮬레이터에서 기기 시간을 자정 직후로 바꿔 빠르게 테스트 가능)

## 📌 데이터 유지보수 (주기적)

- [ ] **CBCK 연도 확장**: 새 해 전례력이 공식 공개되면 `dart run tool/import_cbck.dart 2025 2028` 재실행 → `cbck_days.json` 갱신 후 재배포. (현재 2025~2026 수록, 2027년~ 는 엔진 자동 폴백)
- [ ] (선택) 엔진 폴백 연도(2027년~) 정확도가 중요하면 `assets/calendar/general.json`의 선택 기념일 로스터 보완.

## 📄 참고 (지금 조치 불필요)

- 독서/복음 **본문은 embed 안 함** — 구절 참조 + "매일미사에서 전문 보기" 링크로 대체(저작권 안전).
- 남은 개발(제가 이어서 가능): 통합 테스트, 설정 화면, 영어, 원격 OTA, 알림/위젯, 스토어 배포 CI.

## 검증
```bash
dart test packages/liturgical_calendar   # 엔진 110개
flutter test                             # 앱 4개
flutter run -d chrome                    # 실제 확인 (2025~2026 공식 데이터)
```

_설계: `docs/superpowers/specs/2026-07-02-...-design.md` · CBCK 임포터: `tool/import_cbck.dart`_
